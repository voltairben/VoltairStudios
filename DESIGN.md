---
name: Voltair Studio
description: A one-page startup web-dev studio site staged as a live deploy console.
colors:
  pane-bg: "#222831"
  amber-bright: "#FF5A36"
  amber-dim: "rgba(238,238,238,0.62)"
  cursor-flash: "#EEEEEE"
  led-green: "#45A947"
typography:
  display:
    fontFamily: "IBM Plex Mono, ui-monospace, Cascadia Code, Source Code Pro, Menlo, Consolas, DejaVu Sans Mono, monospace"
    fontSize: "clamp(1.0625rem, 7dvmin, 4rem); clamp(1.0625rem, 6.5dvw, 9rem) from 700px"
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: "normal"
  body:
    fontFamily: "IBM Plex Mono, ui-monospace, Cascadia Code, Source Code Pro, Menlo, Consolas, DejaVu Sans Mono, monospace"
    fontSize: "clamp(0.8125rem, 2dvmin, 1.5rem)"
    fontWeight: 400
    lineHeight: 1.3
    letterSpacing: "normal"
  label:
    fontFamily: "IBM Plex Mono, ui-monospace, Cascadia Code, Source Code Pro, Menlo, Consolas, DejaVu Sans Mono, monospace"
    fontSize: "clamp(0.625rem, 1.2dvmin, 0.8125rem)"
    fontWeight: 400
    lineHeight: 1.3
    letterSpacing: "normal"
spacing:
  1: "clamp(4px, 1dvmin, 8px)"
  2: "clamp(8px, 1.8dvmin, 16px)"
  3: "clamp(14px, 2.6dvmin, 28px)"
  4: "clamp(16px, 5dvmin, 64px)"
  5: "clamp(20px, 4dvmin, 48px)"
components:
  cta-prompt:
    backgroundColor: "transparent"
    textColor: "{colors.amber-bright}"
    padding: "0.4em 0"
  cta-prompt-hover:
    backgroundColor: "rgba(0,173,181,0.08)"
    textColor: "{colors.cursor-flash}"
  flag-link:
    backgroundColor: "transparent"
    textColor: "{colors.amber-bright}"
  flag-link-hover:
    backgroundColor: "rgba(0,173,181,0.08)"
    textColor: "{colors.cursor-flash}"
---

# Design System: Voltair Studio

## Overview

**Creative North Star: "Deploy Console"**

The whole page is staged as one live terminal pane — the studio's own daily material, not a category-default scrolling agency portfolio. A visitor reads it the way a developer reads a CI log: a boot line, a live prompt. The zero-scroll constraint isn't a limitation routed around; it's the concept. A terminal window is inherently a bounded pane, so committing to that world makes "one viewport, nothing more" the honest shape of the thing rather than a workaround.

The hero content is deliberately spare: `voltair_studio` label → one dominant headline → one tagline → the contact prompt, top-left anchored with the rest of the viewport left open to the skybox. A denser version (a three-line service manifest between tagline and CTA) was built, then cut — explicit direction, reference-informed: minimal text, generous negative space, over a fuller pitch. Content that gets added back here should earn its place against that bar, not just fit the available space.

Color originally shipped monochrome-phosphor (one amber family, no second hue); a direct request later replaced it wholesale with a four-color palette (dark navy/slate ground, teal accent, off-white highlight), then the accent alone was swapped again — teal to Warm Persimmon (`#FF5A36`) — chosen over three other offered candidates specifically for contrast against the actual skybox imagery (see Colors). What survives every repaint: the brand mark's green stays confined to exactly one place — the real logo image, now shown top-right in the chrome bar (it used to instead drive a simulated "online" LED dot, since retired) — never a background, never a second theme color, so the terminal's own-world stays uncontaminated by brand color, whatever the rest of the palette is doing around it.

No second display face, no serif/sans pairing: one monospace family carries the headline, the body copy, and the labels, because splitting fonts would dilute a concept built entirely on terminal authenticity.

**The console is a viewport, not a repaint.** A live, switchable 360° skybox (five equirectangular panoramas, slowly rotating) renders behind the terminal, added deliberately without changing the terminal's own materials: still flat, still no drop shadows on its components, whatever the active color palette is. The resolution that keeps this from contradicting the flat/no-photographic-material commitment: the sky isn't part of the terminal's material language, it's what the terminal is a HUD viewport onto — the same way a cockpit display stays flat and legible regardless of what's happening outside the glass.

The content row (label, headline, tagline, CTA) carries no panel or scrim behind it at all — the sky shows through directly behind every glyph, by explicit request. Legibility instead comes from a per-glyph text-shadow halo (`--text-halo`, applied to every text element in `.content`), the same technique video subtitles use: a tight sharp ring right at each glyph, a wider soft glow a few px out, a slight downward drop for depth. This was a deliberate trade, not an oversight — a solid scrim (tried first) measured a guaranteed WCAG contrast ratio; a glyph halo does not have an equivalent flat-color metric, so its legibility was verified visually against all five skyboxes rather than pixel-computed. The window-chrome bar and status bar carried their own translucent background for a while (thin HUD strips, distinct from the open hero space) — later removed too, also by direct request: every text element on the page now floats directly on the skybox with no background of any kind, legibility entirely carried by the halo.

**Key Characteristics:**
- Dark navy/slate ground, one persimmon accent, off-white highlight — not the original amber-monochrome, and not the teal that followed it either (see Colors: every repaint here is a direct, explicit request, not a drift)
- Single monospace type family end to end, no display/body split
- Three-row bounded grid (chrome bar / content / status bar), never a scrolling page
- Motion is a one-shot boot sequence plus a persistent cursor blink, not scattered hover flourishes

## Colors

Replaced wholesale by direct request, exact hex values given: `#222831` / `#393E46` / `#00ADB5` / `#EEEEEE`. The original palette was amber-monochrome (one phosphor color only, no blue, no gray) — that constraint is superseded, not layered on top of. A second, narrower request then swapped the accent alone (`#00ADB5` teal → `#FF5A36` persimmon), picked from four offered candidates on a concrete functional basis: the skybox imagery is predominantly blue/navy, so a blue- or violet-leaning accent (two of the four options) would sit close in hue to its own background and lose contrast; a mint/green option was ruled out by "The One Green Rule" below; persimmon is blue's complement, giving the most actual contrast against every current sky. A third pass then moved the system from "persimmon everywhere" to a real hierarchy — a named, deliberate request, not a taste drift — introducing two genuine slate neutrals (not alpha tints of the accent) and confining persimmon to a short, explicit list of elements it's allowed to touch. Everything still routes through CSS custom properties; the token *names* below (`amber-bright`, `amber-dim`, `cursor-flash`) are legacy from the original phosphor system and now hold neither-amber values — a known naming/value mismatch, not a bug, flagged here so it isn't mistaken for one.

### Primary
- **Accent Persimmon** (`#FF5A36`, token `--color-amber-bright`): used selectively, by explicit rule, not as a general-purpose "the offer" color anymore. Its entire budget: the word "Designers" in the headline, the headline's blinking cursor, and the "Contact" link (both at rest and its own accent role). Everything that used to be persimmon by default — the tagline, the CTA at rest, the footer flags — moved to a neutral below instead.

### Secondary
- **Muted Slate** (`#94A3B8`, token `--color-amber-dim`): the tagline/sub-headline and all status-bar metadata (availability, timezone, copyright) and the "About" label — supporting text that shouldn't compete with the H1 or the CTA. A genuine named slate now, not an alpha tint of white the way earlier versions of this token were.
- **UI Muted** (`#64748B`, token `--color-ui-muted`, underline `rgba(100,115,139,0.35)`): the footer's real links only — `--github`, `--x`, `--linkedin`, the skybox switcher — at rest. Transitions to Accent Persimmon on hover/focus/active. A third, distinct neutral from Muted Slate because these are interactive (need a hover destination to transition *to*) where the tagline/metadata are static.

### Tertiary
- **Base Light** (`#F8FAFC`, token `--color-cursor-flash`): the studio's high-identity elements at rest — the chrome-bar wordmark, "Creative" (the headline's first word), and the email CTA — plus the shared hover/focus highlight for elements whose *rest* state is something else (flags, "Contact"). Two roles sharing one token because they're visually identical (near-white) and this system keeps to few tokens, many uses, as elsewhere.

### Neutral
- **Dark Navy** (`#222831`, token `--color-pane-bg`): the base ground the whole palette is built from. Shows directly only where no skybox-facing surface sits; also the color the content row's text-halo is built from.
- ~~Chrome Glass~~ **retired**: the window-chrome bar and status bar used to carry a translucent `#393E46`-based background (`backdrop-filter: blur(6px)`). Removed by direct request — both bars are now fully transparent, their text (wordmark, nav, status text, flag links) floating directly on the sky like `.content`'s, protected by the same `--text-halo`, not a background.
- ~~LED Green~~ **retired**: `#45A947` used to drive a simulated "online" status LED, the studio logo's color borrowed for a decorative dot. Removed by direct request along with the LED itself.

### Named Rules
~~The One Green Rule~~ **retired, superseded rather than broken**: the logo mark itself (top-right in the chrome bar) is now recolored to Accent Persimmon via `mask-image` — the source PNG's green survives only in the file on disk and the browser favicon, not anywhere the visitor's eye actually lands on the rendered page. A direct, explicit request, not a drift: this system now has zero green on-page, which technically satisfies "green never becomes a site color" even more completely than the rule's own original mechanism did, but the rule's premise (the logo shows its real color *somewhere*) no longer holds, so it's retired rather than left standing on a foundation that's gone.

~~The One Phosphor Rule~~ **retired** — it described the amber-only constraint this repaint superseded. No replacement single-hue rule stands in for it; the four given colors (plus alpha variants of them) are the complete budget, and nothing else should be introduced without the same kind of explicit direction this repaint came with.

## Typography

**Display Font:** IBM Plex Mono (with `ui-monospace, Cascadia Code, Source Code Pro, Menlo, Consolas, DejaVu Sans Mono, monospace`)
**Body Font:** IBM Plex Mono — same family, no second face
**Label/Mono Font:** IBM Plex Mono — same family throughout; this system has no non-mono face

**Character:** IBM Plex Mono was commissioned as IBM's own systems/console face — a literal match for a deploy-console world, not a generic code font pressed into decorative service. Open counters and a taller x-height than most monospace alternatives keep the 10–12px status-bar floor legible, and its ligature-free rendering guarantees `--` prefixes and CLI-style copy display as written rather than merging into a glyph.

### Hierarchy
- **Display** (700, two-tier, line-height 1.3): the headline — the hero's dominant lead element, now two stacked short lines ("Creative" / "Designers", ≤9 chars each) rather than one long line. Below 700px viewport width: `clamp(1.0625rem, 6dvmin, 3.5rem)`. From 700px: `clamp(1.0625rem, 5.5dvw, 7.5rem)`. Raised twice earlier in this system's life, then trimmed back down by direct request ("too big") to the current slope/ceiling — the floor (`1.0625rem`) has stayed untouched through every pass since it's the safety-critical end (320px fit); only the ceiling/slope move, and shrinking them can only reduce overflow risk, never add it, unlike raising them. Each raise was re-verified with real `getBoundingClientRect()` checks against the viewport, not just a scrollbar-presence check, per the lesson below. **The dvw switch (wide tier) is load-bearing, not a preference**: an earlier single-formula version used `dvmin` at a steeper slope for reference-matching scale everywhere, and it silently clipped the headline at 768×1024 — a tall-narrow viewport where `dvmin` resolves to the width, but a `dvmin`-based formula doesn't know this particular text's overflow axis is specifically width. `dvw` ties the size directly to the dimension that actually constrains a `nowrap` line; `dvmin` remains correct for elements whose constraint is genuinely the smaller of the two axes. Every clip failure here was invisible to a scrollbar check (`overflow: hidden` on ancestors hides them) — caught only by comparing the element's own `getBoundingClientRect()` against the viewport, or by opening the screenshot. Any future increase to this token should get the same check, not just a scrollbar-presence test — and note that the *safe* slope for this token is directly tied to the longest line's character count: it got measurably safer to raise once the combined 18-char line split into two lines of ≤9 chars, and would need re-verifying downward if the copy ever gets longer again.
- **Headline entrance (`ScrambleText.tsx`):** both words decode in on mount — random glyphs from a fixed character set (`!<>-_\/[]{}—=+*^?#`) resolve left-to-right into the real text over ~450ms, "Designers" staggered 150ms behind "Creative". A client component; SSR/first paint renders the real text plainly, the scramble is a client-only enhancement layered on after mount (never the only way to read the words), and it checks `prefers-reduced-motion` itself and skips straight to final text — no separate reduced-motion override needed elsewhere for it. `aria-label` on the outer span holds the stable text throughout; the scrambling inner span is `aria-hidden`.
- **Hover distortion:** while the pointer stays over either word, its glyphs continuously re-roll from the same scramble character set (~55ms per tick, independent per word — hovering "Creative" never touches "Designers"), reverting to the real text the instant the pointer leaves. Same `prefers-reduced-motion` gate as the mount decode — no effect at all when that's set. Not a hover state a keyboard/focus user gets an equivalent of; flag if that parity is ever wanted, this is mouse-only by nature (a decode "distortion" isn't a meaningful focus-ring substitute).
- **"Creative" fill:** vertical gradient, white (`#ffffff`) to light slate (`#cbd5e1`), via `background-clip: text` on the ScrambleText inner span (`.type-target .scramble-glyphs` — targeting the inner span specifically, since `background`/`background-clip` don't inherit through elements the way `color` does). "Designers" stays solid Accent Persimmon with its ambient glow (see Colors) — the gradient is exclusive to "Creative," a one-time exception to "no gradients" below, scoped to this single element.
- ~~Micro Label (`.hero-label`)~~ **retired**: a duplicate `voltair_studio` byline once sat directly above the headline, distinct from the chrome-bar's own wordmark. Removed when the chrome-bar's decorative dots were replaced by that same wordmark moved to the top-left — the two occurrences would otherwise have sat stacked right on top of each other. One `voltair_studio` now, top-left of the chrome bar, at `--fs-chrome` size.
- **Title** (600, `clamp(0.9375rem, 2.2dvmin, 1.75rem)`, line-height 1.3): the CTA prompt — the page's single button-like element.
- **Body** (500, `clamp(0.8125rem, 2dvmin, 1.5rem)`, line-height 1.3): the one-line position/tagline statement.
- **Micro** (400/500, `clamp(0.75rem, 1.6dvmin, 1rem)`, line-height 1.3): status-bar text (chrome-bar text is its own, slightly larger tier just below). Raised from `clamp(0.625rem, 1.2dvmin, 0.8125rem)` (10px→13px) by direct request — read as "really small." Chrome-bar text (`--fs-chrome`) similarly raised, `clamp(0.8125rem, 1.8dvmin, 1.125rem)` (13px→18px), up from `clamp(0.6875rem, 1.4dvmin, 0.9375rem)` (11px→15px).

### Named Rules
**The dvmin Rule.** Every fluid size is `clamp(floor, N·dvmin, ceiling)`, never a `vw`-only or `vh`-only formula. `dvmin = min(dvw, dvh)` is what lets one token stay overflow-safe whether a phone is portrait (width-bound) or landscape (height-bound) — the mechanism that makes the zero-scroll constraint hold without a breakpoint table.

## Layout

A fixed three-row CSS Grid filling `100dvw` × `100dvh` exactly — `grid-template-rows: var(--chrome-h) 1fr var(--status-h)` — with `overflow: hidden` on `html`, `body`, and the grid root as a backstop, and `overscroll-behavior: none` to suppress iOS Safari's rubber-band bounce (which can visually reveal an edge even with zero real overflow). `env(safe-area-inset-*)` padding on the grid root handles notched devices, gated by the `viewport-fit=cover` viewport export.

The content row anchors its contents to the top-left via flexbox (`align-items/justify-content: flex-start`), not centered — a deliberate match to the reference's composition: the text block sits at the top, the rest of the viewport stays open to the skybox. No max-width cap: every line is short and `nowrap` by design, so at very wide viewports the block simply keeps its top-left position with more open sky to its right and below, rather than stretching or re-centering.

Three content-swap breakpoints (`380px`, `480px`, `720px`) govern only the status bar's secondary detail — the one row allowed to drop content under width pressure. Narrowest first: only the skybox switcher shows below 380px; `--github`/`--x` join from 380px (added when raising `--fs-status` made the previous fixed set overflow 320px — see the Grid Blowout Lesson's sibling note below); copyright joins from 480px; timezone and `--linkedin` join from 720px. The headline and CTA never compress below their type-scale floor; the fit budget was solved against the narrowest supported viewport (320×568) first.

### Named Rules
**The Grid Blowout Lesson.** `.page`'s `display: grid` declares `grid-template-rows` but must also pin `grid-template-columns: minmax(0, 1fr)`. Without it, the single implicit column auto-sizes to its children's *max-content* contribution (nowrap flex-row text), which can exceed the grid container's own fixed `100dvw`/`100dvh` box — caught at 320×568, where the chrome bar, content row, and status bar all measured 336px inside a 320px viewport even though no individual child actually needed the extra 16px. `.page`'s own `overflow: hidden` clipped the paint silently instead of producing a scrollbar, so `document.documentElement.scrollWidth` read a clean 320 the whole time — invisible to a scrollbar-presence check, caught only by comparing a real child's `getBoundingClientRect()` against the viewport. `minmax(0, 1fr)` floors the column at 0 instead of content size, letting the container's real width win. Any future `display: grid` added to this system should set an explicit column track for the same reason.

## Elevation & Depth

Flat by design — this is a terminal skin, and terminals don't have drop shadows. `box-shadow` isn't used anywhere anymore: its one prior use, the "online" LED's soft green pulse, was retired along with the LED itself. `text-shadow` (the `--text-halo` token) is the system's one non-flat device — functional, not decorative: it's what keeps every text element legible with no background of any kind behind it. No card, panel, bar, or button in this system casts a shadow of any kind, and nothing has a background.

### Named Rules
**The Small-Text Halo Lesson.** `--text-halo`'s 6px/14px blur radii were tuned by eye against the headline's ~90-140px glyphs and never re-checked at smaller sizes. Reported directly ("check these texts as well," a screenshot of the status-bar flags with a visible tinted box behind them) and reproduced live against the bright "morning" skybox specifically — confirmed by disabling `text-shadow` on one flag and comparing it to its still-affected siblings, the box vanished only there. At 12-28px text with normal monospace letter-spacing, those same blur radii are wide enough to bridge the gap between adjacent letters, so every glyph's own halo overlaps its neighbors' and a whole word or sentence blends into one solid-looking box instead of a per-glyph ring — it showed up on nearly every non-headline text element on the page (wordmark, nav, tagline, CTA, footer, index list), not just the flags first pointed out. Fixed with `--text-halo-small`, the same three-layer structure (tight ring, soft glow, slight drop) with radii roughly quartered so they stay inside a single glyph's own footprint — applied to every element below headline scale; `.headline`/`.headline-line2` keep the original `--text-halo` unchanged, since large glyphs never showed this failure mode.

**The Block-Glyph Halo Lesson.** `--text-halo`'s blur layers are tuned for thin letter strokes — traced around a glyph that's already a large solid shape (the headline cursor's `▌`, U+258C), the halo reads as a visible dark box rather than a legibility ring, since there's no thin edge for the blur to hug, just the glyph's own full rectangular outline. Direct request surfaced this as "black boxes behind texts" on the live page. Fixed by explicitly canceling the inherited halo on `.cursor` (`text-shadow: none`) rather than touching the token or any other element using it — a solid, saturated, large block of the accent color is already highly visible against the sky on its own, unlike thin text strokes, so it doesn't need the halo's help. Any future large/solid glyph-like element (not running text) should get the same treatment, not inherit `--text-halo` by default.

**The No-Shadow Rule.** `box-shadow` has no live use in this system; `text-shadow` is load-bearing only for glyph legibility over open sky, never decorative elevation. Nothing in this system frames itself with a background — legibility is carried entirely by the text halo.

**The Gradient-Text Halo Lesson.** `text-shadow` and `background-clip: text` don't compose safely: `text-shadow` paints from the glyph's pre-clip shape, so even a reduced-opacity halo visibly muddies a light gradient fill underneath it (measured directly on "Creative" — any `--text-halo`-derived `text-shadow` value dulled the white/slate gradient to grey; only removing `text-shadow` entirely restored a crisp fill). `filter: drop-shadow()` operates on the already-clipped, post-composite pixels instead, so it shadows behind the visible glyph without touching its fill color — that's what `--text-halo-filter` on `.type-target .scramble-glyphs` uses, and it's the correct tool for legibility shadows on any future gradient-filled text in this system, not `text-shadow`.

## Shapes

No radius token exists in this system, and no `border-radius: 50%` circle either now that the "online" LED (the only one) is gone — every remaining element is a hard-edged rectangle or the logo's own imported shape. No border, either: the CTA carried a bordered-button treatment earlier in this system's life, removed deliberately (a direct request, not a regression) in favor of the same color-wash affordance the status-bar flags already used. There is no card, box, or soft/rounded language anywhere in this system.

## Components

### Buttons
- **Primary (`.cta`):** no border, no fill, no bracket glyphs — Base Light text at rest (`contact@voltairstudio.com`, a real address, given directly — not the earlier placeholder), reading as "distinctly clickable" against the surrounding Muted Slate tagline through brightness alone, not a persimmon default anymore. `min-height: 44px` held via padding alone at every font-size floor for the WCAG 2.5.5 touch-target minimum.
- **Hover/Focus/Active:** text shifts Base Light→Accent Persimmon (the inverse of most links on this page, since the CTA's *rest* state is already the bright one) — `rgba(255,90,54,0.08)`/`14%` background wash, focus-visible outline in persimmon.

### Links (status-bar flags: `--github` / `--x` / `--linkedin` / skybox switcher)
- **Style:** UI Muted text at rest, `rgba(100,115,139,0.35)` underline at 1px, `text-underline-offset: 0.15em` — deliberately quiet until touched, so the footer doesn't compete with the hero for the visitor's one persimmon-trained eye.
- **Hover/Focus/Active:** text and underline shift UI-Muted→Accent Persimmon, with a three-tier persimmon background wash; `transition: 120ms ease` on color/background/underline-color only — no transform, no scale (a terminal doesn't do 3D press effects).

### Nav Link (`.chrome-nav-link`, "Contact")
A third pattern, distinct from both above: Accent Persimmon at rest (explicitly named as one of persimmon's three permitted uses), shifting to Base Light on hover/focus/active — the one nav element meant to already draw the eye before any interaction.

### Navigation
The window-chrome bar now carries the page's only real navigation: `voltair_studio` wordmark top-left (the decorative traffic-light dots that used to sit there were removed by direct request — no function, they never did anything — and the wordmark took that spot instead of its earlier center placement); top-right, the studio logo — recolored to Accent Persimmon via CSS mask, not its native green, matching "Contact" beside it by direct request (see Colors) — an "About" label (non-interactive — there's no About section on this one-page site to link to, flagged as a placeholder — underlined to match "Contact" beside it by direct request, but in Muted Slate's own underline tone rather than persimmon, since it isn't actually clickable), and a "Contact" link (real `mailto:`, reusing the same address as the hero CTA). The "online" status LED this replaced is retired — see Colors. Five real interactive elements now: the CTA, "Contact", and up to three status-bar flags, in visual reading order, left to right, top to bottom.

### Project Reel (`ProjectReel.tsx`, placeholder content)
A vertically auto-looping column of project thumbnails beside the hero text, positioned toward the screen's middle third rather than pinned to either edge — adapted from a reference (a similar site's own center image column), matched to it closely by direct request after a side-by-side comparison: no visible divider between tiles (a 1px pane-bg border, tried first, still read as a visible seam against the diagonal placeholder pattern — removed entirely; the reference's own stacked photos touch with zero line between them) and a widened/quieted stripe pattern (14px/24px bands, up from 10px/20px) that reads calmer, closer to the reference's own understated tone — still a deliberate placeholder signal, not real photography (PRODUCT.md: nothing gets fabricated). `.hero-row` (a new `grid-row: 2` flex wrapper, replacing `.content`'s direct grid placement) holds `.content` and `.project-reel` side by side, separated by a fluid `gap: clamp(24px, 8dvw, 140px)` — the gap plus the reel's own `clamp(200px, 24dvw, 380px)` width (broadened from `clamp(160px, 20dvw, 320px)` by direct request) is what lands it near-center instead of flush against either the text or the viewport edge. Hidden below 700px (the same tier the headline's `dvw` formula switches on) — no real room for a second column next to the hero text at narrower widths without risking the zero-scroll fit budget, so mobile keeps the single-column hero unchanged.

**Autoplay that never stops, plus real scroll/drag — one unified JS-owned position, not two independent systems layered together.** This component went through a real journey: CSS auto-loop → `overflow-y: auto` user-scroll → a full scroll-snap carousel with an `IntersectionObserver`, keyboard nav, and pointer-drag (built to a detailed spec) → that carousel with an auto-wrap at the end → a "loop around, not a scroll back" version of that wrap → a plain CSS auto-loop with no interaction (matching the reference exactly — checked directly, twice: segerman.dev's own image column has no scrollable container, no CSS animation, and doesn't respond to any input, near-certainly canvas/WebGL-rendered like the loading screen's own 3D globe) → a version that layered native `overflow-y: auto` back on top of that same CSS `@keyframes` loop, reasoning that a `transform` doesn't affect scrollable layout so the two would just compose for free. **That composition had a real bug, caught from a user screenshot**: the CSS loop's own `translateY` range (0 to -50% of the track) was tuned assuming nothing else was offsetting the content, but a real `scrollTop` is a second, independent, unbounded offset stacking on top of it — at the combined worst case (a deep scroll plus the animation near its own extreme) the two together could exceed the track's actual duplicated height, exposing blank sky where there was no more content to render. A hard cutoff, not a smooth fade — visible directly in the report.

**The fix**: `.project-reel-track` no longer has a CSS `animation` at all. `ProjectReel.tsx` now owns a single `offsetRef` in JS that auto-advance (a `requestAnimationFrame` loop), `onWheel`, and `onPointerMove` (drag) all move together, applied via `track.style.transform` directly. Safety comes from wraparound, not from bounding the offset: the item list is rendered `REPEAT_COUNT = 3` times, and after every update `wrapAndApply()` keeps `offsetRef` inside `(-2 × singleSetHeight, 0]` by jumping a whole copy-height at a time — invisible, since every copy renders identically — so the position can never drift far enough to run out of duplicated content, regardless of how much auto-advance, scrolling, and dragging accumulate together. Verified by deliberately reproducing the reported failure mode: 30 rounds of aggressive alternating wheel scroll, then 60 sustained ticks in each direction (far beyond one full cycle) with the auto-advance loop running the entire time, sampling every observed tile's actual rendered rect on every tick and checking for any uncovered row inside the reel — zero gaps found across all of it, versus the previous version's real, reproducible failure.

**A second real bug surfaced building the drag handler itself**: `setPointerCapture()` was first called unconditionally on every `pointerdown`, which — once captured — redirects the resulting `click` to the *capturing* element (the reel container, with no `href`) instead of the actual `<a>` tile underneath. This silently broke every real navigation (confirmed directly: a click listener added to every tile never fired at all). Fixed with a drag threshold (`DRAG_THRESHOLD_PX = 5`) — a `pointerdown` only records a starting position; capture only happens once `pointermove` has actually exceeded that threshold, so a plain click/tap never captures the pointer at all and reaches the link exactly as if no drag handling existed. Re-verified after the fix: a click listener on the tiles fires and real navigation works, *and* a genuine drag gesture still moves the track and doesn't trigger navigation.

**"Terminal Glass" tiles**: `.project-reel-item`'s background is translucent now (`--color-glass-fill`/`--color-glass-fill-faint`, both alpha-tinted from existing hex tokens the same way `--color-amber-tint-08` already is) with `backdrop-filter: blur(8px)` and a `--color-glass-border` edge — the rotating skybox behind the reel visibly blurs/refracts through each tile as it drifts, verified directly (computed `backdropFilter`/`border`/`background` all confirmed applied, screenshot shows soft blurred stars through the tiles). Retargeted here from a standalone floating circle (see Colors, above) and, separately, redirected away from a request to put the same glass behind the *hero text container* — that one really would have reversed the halo-over-panel decision this system fought for; these tiles already had their own background, so making it glass is a real upgrade with no such conflict. Still the same diagonal placeholder pattern underneath, just translucent — same "content goes here" signal, not a flat panel standing in for a photo. This does reintroduce a visible per-tile edge, undoing the earlier explicit "no divider, zero seam" choice — a small, real tension, accepted since a pane of glass having a defined edge is inherent to what was asked for. No `box-shadow` (No-Shadow Rule intact).

**The Stale-Measurement / Aspect-Ratio-Bound Lesson.** Reported directly with a screenshot: the loop stopped looping — a hard cutoff below the last visible tile, then a jump back to the start. Two separate, real problems, both fixed rather than just one patched: (1) `singleSetHeightRef` (the one duplicated copy's height, everything else is computed from it) was measured *once at mount* with nothing keeping it current — a window resize, browser zoom, DPI change, or a font-load reflow can all change it afterward, since each tile's height is `aspect-ratio`-derived from the reel's own fluid width. Fixed with a `ResizeObserver` on the track that re-measures and immediately re-normalizes the current offset whenever the track's real rendered height actually changes, closing the whole class of "cached value drifted from reality" bugs rather than chasing the one specific trigger (which automated reproduction couldn't pin down definitively). (2) A second, independent bug in the wrap math itself, found by hand-deriving the real constraint rather than trusting the existing margin: the old bound (`-2 × single`) implicitly assumed one duplicated copy's own height is always comfortably bigger than the reel's visible height — true at ordinary aspect ratios, but the reel's *width* (and so each tile's height) scales with viewport width while its *visible height* scales with viewport height, independently; a narrow-but-tall window can shrink a single copy below the reel's own clientHeight, which a fixed multiple of `single` alone can never safely account for regardless of its value. Fixed by computing the wrap bound from the *actual measured* `reel.clientHeight` and total duplicated track height (`minOffset = clientHeight - trackTotalHeight`) instead of an assumption about their relationship, and `REPEAT_COUNT` doubled (3 → 6) for real margin. Verified by deliberately constructing the exact scenario the math predicted would break it — 750×1300 up to 800×2000 (narrow width, extreme height) — plus the full standard stress suite (alternating scroll, 60-tick sustained scroll each direction, drag, a mid-session resize) at a normal viewport: zero gaps found across all of it.

`touch-action: none` on `.project-reel` stops the browser's own touch-pan from fighting the manual pointer-drag handling (there's no native scroll left for touch to drive). No visible scrollbar exists anymore either, since there's no native scroll container. `prefers-reduced-motion` skips starting the `requestAnimationFrame` auto-advance loop entirely — wheel and drag remain fully functional and call `wrapAndApply()` directly regardless, since manual input is user-driven, not ambient motion.

One known limitation, unchanged from before: the index highlight still derives from the *auto-advance* portion of the position (elapsed time relative to `CYCLE_MS`), not from wheel/drag adjustments layered on top of it — so after scrolling or dragging by hand, the highlighted index item can visibly lag what's actually centered. Correctly accounting for manual input in the highlight calculation too is a real, separate problem, flagged rather than silently shipped as "fully synced" when it isn't.

**No new dependencies**: this project has no Tailwind and no Framer Motion (checked directly, not assumed) — plain CSS tokens, vanilla React, and native browser APIs cover everything built here, at every stage this component went through. `three.js` for the skybox remains the one deliberate exception to "no new dependencies" in this system.

**Real navigation, not just decoration** (`app/data/projects.ts`, `app/work/[slug]/page.tsx`): each tile is a real `next/link` to `/work/[slug]` — confirmed by inspecting the reference's actual DOM (not assumed) that its own project tiles (`<a class="project" href="/work/estrela">`) and index entries (`<a class="project-title" href="/work/estrela">`) are genuine links to case-study pages. `app/data/projects.ts` is the single source of truth (slug + name) that the reel, the index, and `/work/[slug]` all read from, so the tiles, the index list, and the actual link destinations can never drift out of sync. The tiles keep moving via the CSS loop, but each one is still a real, clickable, hoverable `<a>` underneath — clicking navigates immediately regardless of where the loop currently has it positioned. `role="region"` + `aria-label="Project previews"`, not `aria-hidden` — real links need to be genuinely reachable and announced.

**The index highlight derives from the same duration the CSS loop uses, not from watching the reel at all.** There's no scroll position or DOM intersection to observe on a `transform`-driven loop, so `ProjectReel.tsx` runs a `setInterval` (200ms tick) computing `elapsed = (now - mountTime) % LOOP_DURATION_MS`, then `Math.floor(elapsed / PER_ITEM_MS)`, and reports that to `ProjectShowcaseContext` — `PER_ITEM_MS`/`LOOP_DURATION_MS` are the same constants the CSS `animation-duration` uses (kept in sync by hand between the two files, since CSS and JS don't share a value here), and both start counting at mount, so they stay in lockstep rather than merely running at a matching period. `prefers-reduced-motion` skips starting this timer too, in the same effect that would otherwise start it — matching the CSS animation freezing.

### Project Index (`ProjectIndex.tsx`, placeholder content)
The reference's far-right "Index" list — project names, right-aligned, vertically centered in the hero row (`justify-content: center`) — pushed to the row's far edge via `margin-left: auto` in `.hero-row`. Reads from the same `app/data/projects.ts` list the reel does — six placeholder names (`Placeholder 1`…`6`) since no real project names exist yet, same non-fabrication stance as the reel.

**Real links, visually synced with the reel's auto-loop.** Each item is a `next/link` to the same `/work/[slug]` destination its matching reel tile points to (confirmed against the reference's own DOM, not assumed, that its index entries are real links too). `.is-active` (bright Base Light, bold) reflects `ProjectShowcaseContext`'s shared `activeIndex` — driven by the reel's own elapsed-time calculation (see Project Reel above), not by watching the index list itself. `aria-current="true"` marks the currently-active one for assistive tech, on top of the visual state. Hover/focus-visible reuse the same Accent Persimmon + tint-wash pattern as `.flag`/`.chrome-nav-link` elsewhere. Same 700px reveal tier as the reel.

### Case Study Pages (`app/work/[slug]/page.tsx`)
A real destination for the reel/index links to point at, matching how the reference's own project tiles work. `app/data/projects.ts` is the single source of truth for valid slugs (`generateStaticParams` pre-renders all six at build time); an unknown slug calls `notFound()`. No real project work exists yet, so this is an honest "Case study in progress — check back soon" page per project — not invented client work, same non-fabrication stance as the placeholder tiles that link here.

**Deliberately outside `.page`'s zero-scroll layout**: a real case study is exactly the kind of content a normal scrolling page suits, not a single fixed viewport — this required a real structural change, not just a new route. The homepage's `html, body { overflow: hidden }` used to be global (`globals.css` is imported once, in the root layout, shared by every route); it was pure "belt and suspenders" redundancy for the homepage (`.page` already self-contains at a fixed `100dvw`/`100dvh` with its own `overflow: hidden`), but left global it would have silently broken scrolling on every case-study page. Fixed by removing it from `html, body` entirely and moving the one part that actually needed to live somewhere (`overscroll-behavior: none`, the iOS rubber-band suppression) onto `.page` itself. Same reasoning moved `SkyboxProvider`/`SkyboxCanvas`/`LoadingScreen` out of the root layout and into `app/page.tsx` directly — the skybox background and the loading intro are homepage-only concepts, not site-wide chrome; `app/layout.tsx` now holds only genuinely global concerns (fonts, metadata, the html/body shell). No visual styling — flat `--color-pane-bg`, no `--text-halo` (there's a solid background behind the text here, not open sky, so the over-imagery legibility technique doesn't apply) — same monospace identity and token set as the homepage, just plain document flow instead of the hero's fixed-viewport grid.

**Next.js version note**: this Next.js version resolves route `params` as a `Promise` (not a plain object, per `AGENTS.md`'s standing instruction to check the version's actual docs before writing route code) — `generateMetadata` and the page component both `await params`, typed via the generated `PageProps<'/work/[slug]'>` global helper rather than a hand-written params type.

### Loading Screen (`LoadingScreen.tsx`, `LoadingShader.tsx`, `skybox-context.tsx`)
A full-viewport overlay (`--color-pane-bg`, `z-index: 10`, above everything else) shown on first paint: the studio logo, masked to Accent Persimmon (same technique and color as `.chrome-logo` — one recolored mark, not a second rendering of it), centered, with a real bytes-loaded percentage beneath it — adapted from a reference (a similar site's own full-screen loader: centered mark + live percentage while its 3D scene assets load). The percentage isn't simulated: `SkyboxCanvas.tsx` fetches the *first* skybox texture manually (`fetch` + `ReadableStream` reader, tracking `loaded`/`content-length`) instead of going through `THREE.TextureLoader` — whose default `ImageLoader` never fires `onProgress` — specifically so this screen can show genuine progress the way the reference's own does. Subsequent skybox switches (via the status-bar switcher) go back through the plain `TextureLoader` path and never re-trigger this screen; it's gated by a `hasLoadedOnceRef`, one-time only. An 8s failsafe timeout dismisses the screen even if the fetch never resolves (offline, blocked request) rather than hang on it forever. Verified end-to-end with real network-throttle tracing (CDP `Network.emulateNetworkConditions`): on a slow connection, the percentage sits honestly at 0% until JS hydration completes enough for the fetch to even start, then climbs smoothly in step with real received bytes to 100% and fades — confirmed correct, not a hang, by tracing actual `dataReceived` events alongside the on-screen text. `prefers-reduced-motion` keeps the screen functional (still covers the loading skybox) but drops the fade — it pops instead of animating out.

**Minimum display duration**: on a fast/local connection the real fetch finished in well under a second, which made the whole screen just flash — direct request: "make it slower so it actually loads." A `MIN_DISPLAY_MS` (2000ms) floor in `LoadingScreen.tsx` paces the *displayed* percentage toward the real one via a `requestAnimationFrame` loop, computed as `Math.min(realProgress, timeElapsed/MIN_DISPLAY_MS * 100)` — it can never show progress ahead of what's actually been received (a real slow connection still displays its own real, slower pace), it only refuses to let a fast load rush past a perceivable minimum. The screen only actually dismisses once both the real fetch is done AND the floor duration has elapsed. Skipped under `prefers-reduced-motion`, which mirrors real progress directly with no pacing.

**Shader background (`LoadingShader.tsx`)**: a flow-field WebGL shader behind the mark and percentage, added on direct request after evaluating two 21st.dev candidates (recommended "Oceanic Currents" over an Aurora-style cursor-reactive one — its blue-black base was tonally closer to this system's own palette, and its ambient flow-field motion matched this project's established "calm, continuous, never gimmicky" motion stance better than the other's interactive ripple). 21st.dev's own component source sits behind an authenticated registry endpoint (confirmed `403 authentication_required` when checked directly) — this is an equivalent flow-field shader built from scratch (fBm + domain warp, the standard public technique for that look) in this system's own tokens from the start, not a recolor of borrowed proprietary source. Raw WebGL1, one full-screen triangle, no dependency — matches the reference's own "zero-dependency" framing; three.js would be overkill for a single fragment shader. Unmounts (frees the GL context, stops its `requestAnimationFrame` loop) once the fade-out transition actually finishes, rather than running forever hidden behind the real page. `prefers-reduced-motion` freezes it on one static frame instead of removing it.

**Instant first frame**: the WebGL shader can only start once React mounts and compiles it, which — a cold dev-server compile especially — is a perceptible moment during which the screen previously sat on a flat color; direct request: "make it directly visible when opening the page." `.loading-screen::before` is a pure-CSS radial-gradient drift (same palette, `blur(70px)`) that paints from the very first frame with no JS dependency at all, sitting at `z-index: -1` behind the canvas; the instant the real shader starts drawing (opaque, no alpha), it naturally covers this placeholder with no crossfade needed. `prefers-reduced-motion` freezes its drift, same as everywhere else.

**Legibility over a genuinely busy background**: the flow field's own palette can produce bright persimmon/near-white patches anywhere, unlike the mostly-dark skybox photography the rest of the site's `--text-halo` was tuned against — so this needed its own verification, not an assumption that the halo pattern would just carry over. A shader-side vignette (`smoothstep`, centered slightly below the true middle to match where the percentage actually sits, not just the geometric center) darkens a real region around the mark and text on top of the halo/drop-shadow the elements already carry. Measured directly (not assumed): sampling a ring of true background pixels just outside the percent text's real, measured `getBoundingClientRect()` — not the geometric center, and not overlapping the glyphs themselves, an early sampling pass that did overlap the glyphs produced a meaningless near-1:1 "white-on-white" reading — across 10 animation frames gave a worst-case raw background contrast of 12.25:1, comfortably above WCAG AA even before the halo's own extra separation.

### Skybox Background (signature component)
A full-viewport, fixed `<canvas>` (`app/components/SkyboxCanvas.tsx`, `three` — the one real runtime dependency this project carries) rendering an inverted sphere textured with one of five equirectangular panoramas (`public/skyboxes/`), camera fixed at the sphere's center. The sphere's own Y rotation advances continuously — one full revolution per 150s, `requestAnimationFrame`-driven — which is what reads as "the camera panning" without the extra complexity of animating the camera's look vector instead. `prefers-reduced-motion` freezes the rotation (static frame, sky still renders) rather than removing the feature. Active skybox is shared client state (`app/components/skybox-context.tsx`, React Context — the minimal tool for two components five tree-levels apart needing one synced value) and switched via `SkyboxSwitcher.tsx`, a status-bar button styled identically to the `--github`/`--x` flags (`--skybox=<name>`). Textures load lazily per selection, not all five upfront. `pointer-events: none` on the canvas — it's backdrop, never a click target.

**Chrono-aware default on load, full 5-way cycle on click.** A request arrived describing work that was largely already built (the switcher already had `onClick`, `cursor: pointer`, and dynamic text — confirmed by reading the actual files before touching anything, not assumed) and asking for a *second*, parallel `skyboxStyle` state alongside the existing one — extended the real `skybox-context.tsx` `active` state instead of creating a competing source of truth for "which skybox is showing." `SkyboxProvider` corrects `active` from a fixed, SSR-safe default to a local-time-aware choice inside a mount-only `useEffect`, specifically *not* computed during the initial render/SSR pass, since `new Date()` there would reflect the server's clock/timezone rather than the visiting user's, risking a hydration mismatch. `next()` (the switcher's click handler) was briefly narrowed to a 2-way toggle — the original spec's literal ask — but that measurably reduced what the button could reach, and once flagged, direct request reverted it: `next()` cycles all 5 `SKYBOXES` again, unchanged from before this feature existed. The chrono-awareness stays scoped to just the *initial* choice on page load.

**Renamed to a real daytime order.** The user renamed the actual asset files (`Skyboxes/` and `public/skyboxes/`) to `morning`/`day`/`dusk`/`night`/`midnight` — `dusk` and `midnight` are the old `alien`/`space` images relabeled (confirmed byte-identical via `cmp`, same file sizes, just renamed — not new artwork). `public/skyboxes/` (what the site actually serves from) had drifted stale from `Skyboxes/` (the source folder) at the time — synced by copying the renamed files over and deleting the two old-named ones, rather than assuming the two folders already matched. `SKYBOXES` now reads `["morning", "day", "dusk", "night", "midnight"]`, the exact requested cycle order — the switcher's `--skybox={active}` text updates automatically from this, no separate change needed there. The old binary day/space chrono check couldn't survive the rename (`"space"` no longer exists as a name) — replaced with `CHRONO_SCHEDULE`, a real 5-way mapping across all 24 hours (`midnight` 0–5, `morning` 5–10, `day` 10–17, `dusk` 17–20, `night` 20–24). Verified directly at all 10 real boundary hours (both edges of every range, not just one interior sample each) via a `Date.getHours()` override injected before load, plus a live click-through confirming zero 404s for the renamed assets and the exact requested order appearing in sequence.

**Fade, not a snap, on switch.** `SkyboxCanvas.tsx`'s texture-swap effect now dips `.skybox-canvas`'s own opacity to `0.15` before loading a *subsequent* texture, restoring it to `1` a frame after the new one has actually painted — a plain opacity dip on the whole canvas, not a true GPU-side crossfade between old/new textures (that would need a custom shader blending two texture uniforms, real extra complexity for a difference the source material offered as an equally-valid simpler alternative to take instead). Deliberately scoped to switches only, not the first load — the loading screen already fully covers that transition, so a fade there would never actually be seen by anyone. `SWITCH_FADE_MS` drives the canvas's `transitionDuration` directly from JS rather than also hardcoding the same number in `globals.css`, avoiding the kind of "two values kept in sync by hand" duplication `ProjectReel.tsx`'s loop-duration constants already have elsewhere in this codebase. Verified directly, not assumed: an unthrottled test's sampling missed the dip entirely (genuinely too fast to catch at that resolution, not broken) — confirmed real under network throttling, which caught a live `0.5587` mid-transition. Skipped under `prefers-reduced-motion`, same as every other motion in this component.

**Mouse-drift camera tilt**: a request for this arrived as a detailed prompt written for a different stack — React Three Fiber, `useFrame`/`useThree`, `.exr`/`.hdr` env maps, `@react-three/drei`'s `MeshTransmissionMaterial` — none of which exist here (checked directly: `package.json` lists only `three`, no drei/R3F; both `Skyboxes/` and `public/skyboxes/` are plain `.png`). Confirmed with the user, then adapted to the real stack instead of silently reinterpreting or complying with instructions premised on files/APIs that don't exist. The listener is on `window`, not the canvas — `.skybox-canvas` keeps `pointer-events: none` unchanged, so the sky still isn't a click target; this only *reads* cursor position, it doesn't receive pointer events itself. The requested "scroll-based parallax" was dropped rather than faked — the homepage has no real scroll for it to read (the whole point of this build), so there was no honest signal to wire it to.

**"Oil-smooth" physics refinement**: a later, correctly-scoped follow-up (genuinely compatible with the real stack this time, no fabricated dependencies) replaced the first cut with `physicsConfig` — one object holding `maxTiltX`/`maxTiltY` (0.1 rad ≈ 5.7°), `lerpFactor` (0.04), and `ambientSpeedX`/`ambientSpeedY` (0.02/0.03) — plus a continuous sin/cos ambient wobble layered under the mouse tilt, so the sky keeps a faint living motion even with the cursor still. `mousePosRef`/`targetRotationRef` (`useRef`, not state — mousemove and the render loop never trigger a React re-render) hold the normalized cursor position and the camera's current lerp target; each frame recomputes the target from `mouseY × maxTiltX + sin(time) × ambientSpeedX` (and the Y-rotation analogously from mouseX + `cos(time) × ambientSpeedY` — a deliberate quarter-cycle phase offset from the X axis's `sin`, so the two axes trace a lazy ellipse instead of a straight diagonal, which is what actually reads as organic rather than mechanical), then eases `camera.rotation` toward it via `+= (target - current) × lerpFactor` rather than snapping straight there. Still nested inside the same `!reduceMotion` branch as the ambient sphere rotation — continuous drift (mouse *or* ambient) is closer in kind to that than to a deliberate click-drag gesture (which elsewhere in this system, e.g. the project reel, stays enabled under reduced motion since it's user-initiated). Texture loading, scene/camera/renderer setup, and resize handling are untouched — this only replaced the tilt-calculation block inside the existing render loop.

~~Glassmorphism accent (`GlassAccent.tsx`)~~ **removed** — a small circular `backdrop-filter: blur()` element that floated in the open lower-left sky, parallaxing with the cursor. Retargeted by direct request into "Terminal Glass" on the project reel tiles instead (see Project Reel below) — the standalone circle is gone, file deleted, `.skybox-glass-accent` CSS removed.

**"Terminal Glass" on the project reel tiles**: a request to apply this same glass material as a background *behind the hero text container* arrived alongside the retargeting ask, framed as "the spirit of the Chrome Glass rule." That's backwards — the Chrome Glass entry above records *removing* a panel from behind text in favor of it floating directly on the sky with a halo; putting glass behind the hero container would reverse that, not honor it. Flagged directly and redirected to `.project-reel-item` instead (see Project Reel below), which already carries its own background and has no such conflict. The **No-Shadow Rule** still holds either way — no `box-shadow` was added; blur + translucency + a 1px border carry the glass read on their own.

**Card-to-page morph, project reel → case study.** A request for a shared-element transition (framed around Framer Motion `layoutId`) arrived for a stack that doesn't have Framer Motion installed. Checked the next-recommended alternative before reaching for a new dependency: Next.js 16.3.2's own docs describe a zero-config native `ViewTransition` component from React — but the installed `react@19.2.8` doesn't export it (confirmed directly: `Object.keys(require('react'))` has no `ViewTransition`; the docs assume a canary build this project isn't on). Rather than bump React off stable for one page transition, added `next-view-transitions` — a small wrapper around the browser's native View Transitions API that works with the installed React, confirmed with the user before installing. `ViewTransitions` wraps the root layout (`app/layout.tsx`); `ProjectReel.tsx` and the case-study back link both swap `next/link`'s `Link` for its drop-in replacement, since only navigations that go through it actually trigger a transition. The project label text in the reel and the `<h1>` on `/work/[slug]` share a `view-transition-name` (`project-title-{slug}`), so the browser morphs one into the other instead of a hard cut.

The real complication wasn't the transition itself, it was `ProjectReel.tsx`'s existing infinite-scroll trick: `REPEAT_COUNT` renders every project 6× simultaneously in the DOM (see Project Reel below), so tagging every copy's label with the same `view-transition-name` would collide — the browser requires that name to be unique within one document snapshot. Fixed by tracking *which physical copy* was actually clicked (`morphIndex`, an index into the duplicated array, not into `PROJECTS`) via `onClick`, and only that one instance ever receives the name; the other 5 render a plain untagged span. The reverse direction (case study → back to the reel) has the same problem from the other side: a fresh mount of `ProjectReel` has no memory of which tile was clicked. Solved by round-tripping the slug through a URL hash (`/#slug`) on the back link — read once in a mount-only effect, matched to the copy index the reel already starts centered on (`PROJECTS.length + projectIndex`, matching the "start in the middle copy" offset the loop math already uses), then the hash is stripped via `history.replaceState` so it doesn't linger in the URL. Verified directly, not assumed: a Playwright pass confirmed the clicked tile's text matches the landed page's `<h1>` text, the back link's `href` carries the right slug, loading `/#placeholder-3` directly tags exactly one `.project-reel-label` (not 6), and the homepage has zero scroll overflow after the round trip. `npm run build` stayed clean, same route table as before.

**Missing "grow and stretch": the flushSync fix.** The morph landed with the navigation working (right page, right project) but not actually growing/sliding — reported directly as "missing the card grow and stretch." Root cause, confirmed by instrumenting `document.startViewTransition` itself rather than guessing: `next-view-transitions`' `Link` calls the app's `onClick` prop, *then* synchronously calls `document.startViewTransition`, in the same event handler. A plain `setMorphIndex(i)` inside `onClick` schedules a re-render but doesn't commit to the DOM before that same handler continues — React defers the paint past the end of the event. So the browser's "old" snapshot was taken with *nothing* tagged yet; the case-study page's `<h1>` had no partner to pair with and just faded in as a new, unpaired element — a crossfade, not a morph, which reads exactly like "the grow and stretch is missing." Fixed by wrapping the state update in `flushSync` (from `react-dom`) in both `ProjectReel.tsx` and `ProjectIndex.tsx`'s click handlers, forcing the commit to finish before the handler returns and `next-view-transitions` calls `startViewTransition`. Verified by wrapping `document.startViewTransition` in a Playwright `addInitScript` probe that records what's tagged in the DOM at the exact instant it's called: before the fix this would show `[]`; after, it shows exactly one tagged element matching the clicked project, for both surfaces.

**Same treatment on the Index.** `ProjectIndex.tsx` shows the same 6 projects as `ProjectReel.tsx`, just without the reel's 6x DOM duplication — so it doesn't have the reel's *same-slug-appears-6-times* problem, but it has a different one: both components render every project *at the same time*, so a static, always-on `view-transition-name` on every index item would collide with whichever reel tile the user actually clicked (same slug, same name, two DOM elements — the browser rejects that). Fixed with a shared `morphSource: 'reel' | 'index' | null` in `project-showcase-context.tsx` (the context already binding these two components together): whichever surface is actually clicked claims it, and the other's tag stays off. The reverse (case-study → back) morph is scoped to the reel only, not the index — the back link always restores into the reel's copy regardless of which surface sent the user there in the first place, a deliberate scope cut rather than double-encoding "which surface" into the URL hash for a placeholder-content page.

**Reduced motion.** The View Transitions API has no built-in `prefers-reduced-motion` check, unlike every other motion source in this codebase (each has its own guarded block). Added one to `globals.css` disabling `::view-transition-group/old/new` animation — content still swaps correctly, just without the grow/slide.

**Hover-grow on the reel tiles.** Separate from the click-triggered page morph above — this fires on mouseover, before any click happens. `.project-reel-item:hover`/`:focus-visible` (already carrying the amber background tint) now also gets `transform: scale(1.06)` plus `z-index: 1`, so the hovered tile visibly grows and lifts above its still-auto-advancing neighbors instead of getting covered by them mid-scroll. Flex items respect `z-index` without needing `position: relative` first (per spec, unlike normal block children), so no extra positioning was needed. `.project-reel`'s own `overflow: hidden` still clips a tile that's scaling right at the visible window's top/bottom edge — the accepted tradeoff of any row of overflow-clipped hover cards, not a reason to widen the reel's clip box. Left ungated by `prefers-reduced-motion`, matching the existing (already-ungated) hover color-tint rule right next to it — same category of bounded, pointer-proximity hover feedback, not the continuous/ambient motion this codebase otherwise gates hard. Verified via computed-style diff (`transform: none` → `matrix(1.06, 0, 0, 1.06, 0, 0)` on hover), not just visual inspection.

**Bigger pop, and sideways.** Two follow-up requests on the hover-grow above: "make it grow even more, I want it to pop out" bumped `scale(1.06)` → `scale(1.16)` and swapped the flat `ease` for a slight-overshoot `cubic-bezier(0.34, 1.56, 0.64, 1)`, so the tile snaps past its resting size on the way in instead of just easing up to it. "Make it go sideways" added `translateX(-14px)` to the same hover transform — shifts left, into the gap toward the headline (the open side of the layout), away from the Project Index list sitting on the reel's other side. That shift needed `.project-reel`'s `overflow: hidden` split into `overflow-y: hidden` (still load-bearing — it's what hides the duplicated copies stacked above/below the visible window) and `overflow-x: visible` (the axis that was clipping the sideways pop before this), rather than just cranking the translateX value and hoping it fit inside the existing box. Verified at every width the reel actually appears at (700px, its own `min-width` reveal breakpoint, through 2560px) that the hovered tile visibly grows and shifts left with zero page-level overflow (`scrollWidth === clientWidth` at all five) — the risk wasn't the transform, it was `.hero-row`'s own `overflow: hidden` or the reel's fixed-width column clipping the pop at the narrowest width, and it doesn't.

**Even more pop.** One more bump: `scale(1.16)` → `scale(1.32)`, `translateX(-14px)` → `translateX(-28px)`. Re-verified at the same five widths (700–2560px) since a bigger shift is the one most likely to finally hit `.hero-row`'s own `overflow: hidden` or the reel column's edge — at 700px (the narrowest the reel ever renders) the tile now grows from 200px to 264px wide and shifts left by roughly 69px total, still zero page-level overflow at every width tested.

**Kinetic variable weight on the headline.** The spec called for removing `next/font/google`'s `weight` array on `IBM_Plex_Mono` to "unlock" a variable axis — checked Next's own bundled Google Fonts data first rather than trying it: IBM Plex Mono is listed there with only 7 static weights, no `"variable"` entry, so omitting `weight` doesn't unlock anything, it throws `Missing weight for font 'IBM Plex Mono'` at build time. The font genuinely can be variable now, though — IBM shipped a real IBM Plex Mono Variable font 3 weeks before this request (`@ibm/plex-mono-variable@1.0.0` on npm, OFL-1.1, wght 100–700), Google's hosted copy just hasn't caught up to it. Confirmed with the user before switching the loading mechanism, then temp-installed the package, copied just the Roman `woff2` out to `app/fonts/`, and uninstalled it again — the package itself pulls in an IBM telemetry dependency the site has no use for shipping. `app/layout.tsx` now loads it via `next/font/local` with `weight: "100 700"` (a CSS weight *range*, telling the browser this one file covers the whole axis, not a single value) instead of `next/font/google`, same `variable: "--font-mono"` name as before so nothing downstream needed to change.

`KineticTypography.tsx` is a behavior-only component (renders `null`) that tracks the mouse via a `window` `mousemove` listener and writes `--weight-creative`/`--weight-designers` onto `:root` inside a `requestAnimationFrame` loop — refs, not state, matching this codebase's existing convention for per-frame values (`SkyboxCanvas.tsx`, `ProjectReel.tsx`). Distance from each word's center maps to `100` (far) through `700` (hovering directly), inverted from what might be the intuitive default — that's the literal spec, and it reads well: the headline gets bolder as attention (the cursor) approaches it. `globals.css` reads the vars with a `var(..., 700)` fallback, and the vars themselves stay unset until the first real `mousemove` fires — without that gate, the headline would render thin (`100`, whatever "far away" resolves to at zero mouse data) for the instant between mount and the user's first cursor movement, a real flash-of-wrong-weight on every load. Leaving the viewport entirely (`documentElement`'s own `mouseleave`, which only fires on a true viewport exit) clears the vars back to that same fallback rather than freezing at whatever weight was last set at the edge. `prefers-reduced-motion` skips binding the listener at all — no JS path even exists to move the weight, so it's a true static `700`, not just a suppressed animation. Verified directly: resting weight `700`, hovering the word `699` (~max, matching direct cursor proximity), far corner of a 1440-wide viewport `100`; reduced-motion stays exactly `700` before and after a hover attempt; zero page overflow at 320×568, 390×844, and 1440×900 with the new font file in place (the fit-math is calibrated close to the 320px floor, so a font-source swap was worth re-checking, not assuming).

**Breaking loose of the bar.** Screenshotted directly: the hover-pop still read as "scoping out" — a washed-out fade right at the top/bottom of a popped tile, not a clean escape. Two separate causes, not one: `.project-reel`'s `overflow-y: hidden` gave a scaled tile zero room to bleed past the box edge before getting clipped (fixed by switching to `overflow-y: clip` + `overflow-clip-margin: 110px` — `clip` behaves the same for hiding the duplicated loop copies, since nothing here ever calls `.scrollTop` on this element, but unlike `hidden` it can carry a bleed margin). The second, subtler cause: `.project-reel` also carries a `mask-image` linear-gradient fading its own top/bottom 10% to transparent, to soften the infinite-loop's cut edges — that's an alpha mask, not a clip, so it was still fading a tile that bled into that zone to see-through even after the clip-margin fix, which is exactly the "washed out" look in the screenshot. Fixed with `.project-reel:has(.project-reel-item:hover)` dropping the mask entirely while anything's actually hovered — the loop-fade still does its job the rest of the time, on the resting reel. Scale bumped again too, 1.32 → 1.6, now that there's real room for it to land in rather than hitting a hard wall harder. Verified: `mask-image` computes to `none` during hover (was the gradient at rest), `overflow-clip-margin` computes to `110px`, hover transform computes to `scale(1.6)`, zero page overflow at 700–2560px with a tile actively hovered at each width, and a screenshot confirms the popped tile renders fully opaque, clearly overlapping past the reel's own column instead of fading into it.

**Shifted right.** `.project-reel` got its own `margin-left: clamp(16px, 3dvw, 56px)`, on top of (not replacing) `.hero-row`'s shared gap — direct request. `.project-index` sits after it in the same flex row with no explicit position of its own, so it rides along automatically; didn't need a separate change. `.hero-row` itself carries `overflow: hidden`, so this got the same overflow/overlap check as everything else that touches this row: verified at 700–2560px that the Index stays fully on-screen and never overlaps the reel, and zero page-level overflow throughout.

~~Terminal CLI, replacing the mailto CTA~~ **built, then reverted.** A full interactive command line (`help`/`work`/`skybox --skybox=<name>`/`contact`/`clear`, arrow-key history, a bespoke blinking cursor reusing the existing `.cursor` class) shipped in this same slot in `TerminalPane.tsx`, complete with its own `setSkybox(name)` addition to `skybox-context.tsx` so a command could jump straight to a named skybox. Removed by direct request shortly after — `TerminalCLI.tsx` deleted, `TerminalPane.tsx`'s `<a className="cta">` mailto link and `globals.css`'s `.cta`/`.cta:hover/:focus-visible/:active` rules restored verbatim, `setSkybox` removed from the context again since nothing else used it. No lesson beyond the plain preference reversal — noted here rather than silently erased, matching how `GlassAccent`'s removal above is recorded.

**Skybox on the case-study pages too.** `SkyboxProvider`/`SkyboxCanvas`/`LoadingScreen` moved from `app/page.tsx` up to the root `app/layout.tsx`, shared across every route instead of duplicated or homepage-only — direct request. Sharing at the root (rather than giving `/work/[slug]` its own independent instance) means navigating between home and a case study doesn't re-fetch the texture, reset to the default skybox, or re-run the loading screen: verified directly via request-count tracking (switching skybox on the homepage fires exactly one new texture fetch; navigating into a case study afterward fires zero more, and the switched-to skybox is still the one showing there). `.page`'s own zero-scroll grid stayed homepage-only in `app/page.tsx` — `.skybox-canvas` is `position: fixed` and `pointer-events: none`, so it doesn't care what layout sits on top of it; `/work/[slug]` keeps its own plain, normal-scrolling flow untouched.

The real bug this surfaced: `.case-study`'s back link, body copy, and CTA rendered completely invisible against the now-present sky — not a contrast problem, genuine occlusion, confirmed by hiding the canvas and watching all three reappear intact. `.case-study` had no `position` set; per CSS stacking order, a plain static element paints *behind* a `position: fixed` sibling regardless of DOM order, and previously that never mattered because nothing sat behind this page. `.page` already carries `position: relative` for exactly this reason on the homepage — `.case-study` just never needed it until the skybox joined it. Same fix applied here. (The title alone stayed visible throughout the debugging, which is what actually pointed at a stacking issue over a color one — it carries a `view-transition-name` for the card-to-page morph, and elements tagged with one get promoted to their own compositing layer by the browser, accidentally exempting it from the normal in-flow paint order the other three were still subject to.) Also added the same `--text-halo`/`--text-halo-small` treatment every other on-sky text element already carries — these were styled for a flat `--color-pane-bg` background and had no halo at all before this.

**3D-tilt mockup card, sourced honestly.** A spec for an "immersive mockup card viewer" arrived assuming high-res project screenshots already existed — none do, for any of the 6 projects, real or placeholder (`PRODUCT.md`'s non-fabrication rule, the same one that made `ProjectReel`'s tiles render labeled placeholder slots instead of invented images). Flagged directly rather than either inventing imagery or silently dropping the feature. Resolved by capturing a *real* screenshot of KrachtigFit's actual live site (the one project with a real URL) via a temp Playwright script, saved to `public/mockups/krachtig-fit.png`, then removed the temp install — an honest source, not a mockup. The other 5 placeholder slots show a plain, clearly-labeled `.mockup-card-empty` ("no preview yet") instead of a fabricated image, same rule as before. `data/projects.ts` gained `mockup`/`year`/`role`/`description` fields, all optional and all unset except `mockup` for KrachtigFit — nothing invented to fill them.

The spec also asked to lock case-study pages into the fixed-viewport zero-scroll HUD grid — reversing a decision this file already documents twice, for the opposite reason, right above this entry. Confirmed rather than silently complied with or silently ignored: kept the normal-scrolling page. The reflow (`.case-study-layout`: text left, mockup right, stacking below 900px) lives inside that unchanged architecture, not instead of it.

`ProjectMockupCard.tsx` — the interactive part reused this system's own established patterns rather than introducing new ones: `--tilt-x`/`--tilt-y`/`--tilt-scale`/`--shine-x`/`--shine-y` are written directly via `ref.current.style.setProperty(...)` on `mousemove` (matching `SkyboxCanvas.tsx`/`ProjectReel.tsx`'s existing ref-over-state convention for continuous pointer-driven values, not React re-renders per pixel of cursor movement), and the glass material reuses the exact tokens `.project-reel-item` already established (`--color-glass-fill`/`-faint`/`-border`, `blur(8px)`) rather than inventing a second glass recipe. `prefers-reduced-motion` is checked once inside the mousemove handler itself and, when active, the handler returns before ever calling `setProperty` — so the CSS custom properties stay permanently unset and the `var(..., 0deg)`/`var(..., 1)` fallbacks govern instead; no separate CSS override needed since there's no JS path left that could move it at all. A card only becomes a real `<a>` (and only then gets a `:focus-visible` outline) when `project.url` exists — a placeholder card with nothing behind it stays a plain visual, not a focusable control that goes nowhere. No `border-radius` anywhere — checked directly (zero uses in the whole stylesheet) before adding a card shape, matching the system's fully sharp-cornered terminal aesthetic rather than assuming rounded corners were fine.

Verified: real screenshot renders and the transform genuinely changes with the cursor (confirmed via computed-style diff, not just visual inspection), resets cleanly on mouseleave, placeholder projects show the empty state with zero images and no focusable link, `prefers-reduced-motion` leaves the transform bit-for-bit identical before and after a hover attempt, keyboard focus produces a solid outline, the layout stacks to a column below 900px and sits side-by-side above it, and zero horizontal overflow at 320–2560px.

**Estrela carousel — real facets, not fabricated app screens.** A spec for a "Landing/Dashboard/Settings/Pricing" multi-page carousel arrived for KrachtigFit, a single-page coaching site with no such screens — checked its real nav/routes directly (`#about`, `#method`, `#programs`, `#faq`, one real route `/kennismaking`) before writing anything, rather than fabricating a Dashboard/Settings that don't exist for this business. Adapted rather than blocked: captured 4 real screenshots of the site's own real sections/pages via a temp Playwright script (Home, "Over mij", "Programma's" — its real pricing-tier equivalent, and "Kennismaking" — a real 5-step intake flow, which honestly demonstrates more interactive depth than a static "Dashboard" mockup would have anyway) and used those, correctly labeled with their real names, not the generic ones asked for.

`data/projects.ts`'s `mockup?: string` became `mockups?: { label: string; src: string }[]` — each entry's `label` is that section's own real name. `EstrelaCardViewer.tsx` replaces `ProjectMockupCard.tsx` outright rather than living alongside it — a carousel is a strict superset of a single static card (1 slide = no dots/arrows rendered at all), so keeping both would just be two copies of the same glass/tilt shell; the file the request named didn't conflict with anything real, so it's the name that survived. The 3D tilt mechanism itself (`--tilt-x`/`-y`/`-scale` via `ref.current.style.setProperty` on `mousemove`, `prefers-reduced-motion` checked once inside the handler which simply stops calling `setProperty` when active) carried over unchanged from the card it replaces — still applies to the whole carousel as one unit, tilting while the slide underneath can be navigated independently.

Carousel mechanics: `activeIndex` is real `useState` (a discrete, click/keydown-driven index — not a continuous per-frame value like the tilt vars, so it doesn't belong in a ref the way those do), the track slides via `translateX(-${activeIndex * 100}%)`. Prev/next are real `<button>`s, always in the tab order — "hover-revealed" is an opacity transition, never `display:none`/`visibility:hidden`, since either of those would remove keyboard users' access to them entirely rather than just visually quiet them. `ArrowLeft`/`ArrowRight` are handled on the card's own `onKeyDown` so they work regardless of which specific child (a dot, an arrow, the region itself) currently has focus. A `.sr-only` live region announces "Schermafbeelding X van Y: {label}" on every change, in Dutch to match the label data itself. Dots are small filled squares, not circles — `border-radius` has zero uses anywhere else in this stylesheet (checked directly, again, before adding anything that might need one), so dots don't get an exception either.

The outer `<a href={project.url}>` wrapper the single-card version had is gone — nesting real interactive `<button>`s (dots, arrows) inside an `<a>` is invalid markup and breaks focus/click handling for both. Not a loss: the case-study page's text column already carries its own separate, real link to the live site (`krachtig-fit-two.vercel.app ↗`), so the "go to the site" affordance still exists, just not doubled up inside the now-interactive viewer.

Verified: all 4 real images present, dot/arrow/keyboard navigation all independently confirmed to move the track and update the active dot correctly (including backward via `ArrowLeft`), the live region announces the right slide, the tilt mechanism still responds to the cursor post-consolidation, placeholder projects render zero images and zero nav controls (not just a visually-empty carousel — genuinely no dots/arrows in the DOM at all), `prefers-reduced-motion` leaves the tilt transform bit-for-bit identical while carousel navigation still works (a direct click, not ambient motion), and zero page overflow at 320–2560px.

**Dark-theme recapture, and a real stale-image-cache lesson.** KrachtigFit's live site has a real light/dark toggle (`aria-label="Donkere modus"`) — checked directly rather than assuming the screenshots were locked to whatever theme happened to be active on first capture. Re-ran the same temp-Playwright capture script with the toggle clicked first, confirmed dark mode persists across a real navigation to `/kennismaking` (no need to re-toggle there), and overwrote the same 4 filenames — still real screenshots of the real site, just the theme actually asked for.

The overwrite alone didn't show up on the live page — a real, worth-recording caching lesson, not a fabrication/architecture issue like the rest of this file. `next/image`'s dev-mode optimizer caches per *format* (`Content-Type` negotiated via the request's `Accept` header), not just per source path: a plain `curl` (no `Accept: image/webp,...`) got served a freshly-regenerated, correctly-dark PNG passthrough, while the browser's actual `<img>` request (which does send that header) kept hitting a stale cached `image/webp` entry — confirmed directly via the response's own `X-Nextjs-Cache: HIT` header, not guessed. `rm -rf .next/cache/images` didn't clear it; a full server restart with `.next/cache` wiped didn't either. Only removing `.next` entirely and restarting fresh forced a genuine `X-Nextjs-Cache: MISS` on the exact request the browser makes. Verified past that point by screenshotting the live rendered element (not just curling a URL) for all 4 slides.

**Curated, not mechanical.** Two direct fixes: one screenshot (Programma's) genuinely was broken — ghosted, doubled text, screenshotted directly by the user — root-caused to a scroll-reveal animation caught mid-transition (the earlier capture used `scrollIntoView` + a fixed wait, not `reducedMotion`), fixed by capturing with `reducedMotion: 'reduce'` this time, which lets a live comparison confirm the fix rather than assume it. The bigger ask was "best places, not random ones" — actually reviewed the whole live page this time (a full scroll-through in 900px increments, every segment looked at) before choosing anything, rather than mechanically following the site's own nav anchors like the first pass did. Swapped one selection for a genuinely better one: added "Klanten resultaten" (the before/after results carousel — tilted 3D photo cards, the single most visually distinct section on the whole site) as a 5th slide, reordered into a deliberate narrative (intro → who's behind it → proof → what you get → how to start) instead of just the order sections happen to appear on the page.

That results section itself needed a real fix to capture correctly — its photo reveal only fires on genuine incremental scroll events, not a `scrollIntoView()` jump to the same final position (confirmed directly: the jump consistently left it in a "not yet revealed" cover-card state, incremental `scrollTo` in steps did not). Verified this round by checking all 5 slides individually via the carousel's own next arrow, not just spot-checking a couple — the exact gap that let the broken Programma's slide through undetected last time.

**Actually centered, not eyeballed.** Screenshotted directly: slides 3 and 4 (Klanten resultaten, Programma's) weren't centered — both had a chunk of dead space at the top and the real content (the cards) cut off at the bottom edge. Root cause: those two captures were framed around the *section*, which is taller than the 900px capture viewport (1136px and 1352px respectively) — versus Home/Over mij/Kennismaking, which happened to fit. Fixed by measuring the real DOM directly (`getBoundingClientRect`, not eyeballing) for the actual visual payload in each — the card grid plus its heading for Programma's (880px, fits), the results carousel plus its review-link bar for Klanten resultaten (842px, fits, heading dropped since carousel + heading together don't fit under 900px and the carousel is the actual point of that slide) — then computed the exact scroll offset that centers that measured block in the 900px frame, rather than an arbitrary anchor-based scroll position. Verified by screenshotting both slides on the live rendered page afterward, not just the source PNGs.

**A "new" global skybox spec that was already fully shipped.** A prompt asked to build root-layout skybox persistence, a prop-driven opacity-dip fade (`SWITCH_FADE_MS`), vanilla-Three.js mouse drift, and a clickable `--skybox=` cycle — all of it already existed, verbatim, from earlier rounds (down to the exact constant name). Confirmed via direct trace rather than rebuilding blind: flagged as already-done, one factual correction (the switcher lives in `StatusBar.tsx`, the prompt named `TerminalPane.tsx`, which has no such string), and no code changed. The prompt's own required first step — check `design/` and `Logo/` for reference material before touching code — did surface two genuine, previously-unseen items though, unrelated to the skybox ask itself:

- `design/DESIGN-SPEC.md`: a complete, pre-existing implementation spec for a since-abandoned early direction (amber/phosphor palette, `$ voltair deploy --launch` headline, a `--design`/`--build`/`--ship` manifest, no skybox/reel/CLI at all) — none of it matches anything actually built. Confirmed with direct request: leave it as-is, not a current source of truth for anything.
- `Logo/Sander1.jpeg`: a real photo of KrachtigFit's trainer, added with no attached instruction. Direct request: it's for the KrachtigFit case study.

**Sander's photo — first placed in the case-study carousel, then corrected to the reel tile.** Copied to `public/photos/krachtig-fit-sander.jpg` — a separate folder from `public/mockups/`, deliberately: it's a real photograph, not a site screenshot, and the two shouldn't blur together under one folder name that implies "screenshot of the live site." First attempt slotted it into `EstrelaCardViewer`'s carousel as a 6th slide — wrong location, corrected by direct request/screenshot: the actual ask was the `ProjectReel`/`ProjectIndex` tile itself, not the case-study page. Pulled back out of the `mockups` array and given its own field instead — `Project.image`, distinct from `Project.mockups` since they're genuinely different slots (one tile thumbnail vs. a whole case-study carousel) with different consumers.

`ProjectReel.tsx` renders it as a real `next/image` `fill` layer behind the tile's label, gated behind a `.has-image` modifier class that drops the diagonal "content goes here" glass pattern and its backdrop-blur entirely for that tile — the pattern's whole meaning is "nothing here yet," which stops being true the moment there's a real photo, so the two were never meant to coexist. A gradient scrim sits between the image and the label (`.project-reel-item-scrim`, `rgba(34, 40, 49, 0.75)` fading to transparent — spelled out from `--color-pane-bg` since a CSS gradient can't take a token inside `rgba()`'s alpha slot) — checked the actual rendered legibility first rather than assuming the halo alone (tuned for the skybox's soft gradients) would carry a high-contrast gym photo with neon signage the way it does everywhere else text sits over imagery in this system; it didn't, so the scrim earns its place. `.project-reel-item` gained `position: relative` (anchors the image fill + scrim) and `.project-reel-label` gained `position: relative; z-index: 2` (an unpositioned sibling paints behind positioned ones by default, which would've buried the label under the image) — the tile's existing hover-pop (scale/translateX/z-index escaping the reel's own clip) needed neither changed nor re-verified in theory, but was re-checked live anyway since it's exactly the kind of interaction a stacking change could quietly break. The photo is portrait (1536×2048) forced into the tile's 4:3 frame built for a text-only placeholder — checked the actual rendered crop rather than assuming: Sander, the "HAMMER STRENGTH" sign, and the dumbbell rack all stay readable at rest, and the crop still reads correctly at 1.6× scale mid-hover-pop. The original file was removed from `Logo/` once copied — `Logo/` holds the studio's own brand assets, not client photography — easy to put back if that removal wasn't wanted.

**Klanten resultaten, recaptured to a reference screenshot.** Direct request with an attached screenshot of the exact desired framing — captured at a much wider real browser viewport (2560px) than the previous 1440px pass, which changes the composition: the carousel's own width is capped, so at 2560px it sits centered with generous gold negative space on both sides, matching the reference exactly. Re-measured the real DOM at that width specifically (layout reflows differently at different viewport widths — text wraps, so absolute Y positions shift) rather than reusing the 1440px numbers, then centered on the heading + carousel together (no review-link bar this time — the reference doesn't show one). Verified on the live rendered page afterward, not just the source file.

**Skybox switcher, now on case-study pages too.** The skybox itself was already shared globally (see the earlier entry above), but the only *control* for it lived in `StatusBar.tsx` — homepage-only. Direct request: still wanted to be able to change it while looking at a project. Reused `SkyboxSwitcher.tsx` as-is (no new component, it's not tied to `StatusBar` specifically — a plain button reading `useSkybox()`), placed in a `position: fixed` corner (`.case-study-skybox`) rather than inline in the page flow, since `/work/[slug]` is a real scrolling page and an inline control would scroll out of reach past the first screen. `z-index: 1` is enough to clear `.skybox-canvas` outright (an explicit non-auto value beats another positioned sibling regardless of DOM order, no need to lean on the DOM-order rule `.case-study` itself relies on). Verified live: clicking it actually changes the active skybox (confirmed by reading the button's own label before/after, not just that it didn't error), and the control stays on-screen after scrolling the page.

**Full chrome/status bar on every case-study page, superseding the one-off skybox button.** "4 screenshots to implement" turned out, after asking, to mean 3 tightly-cropped images of this system's own current chrome bar and status bar — not new project content. The actual ask: a visitor should always have access to the same nav/flags/skybox switcher, in the same top/bottom position, regardless of which page they're on. `/work/[slug]` had neither before this — only the previous round's one-off fixed skybox button. `ChromeBar`/`StatusBar` are reused completely as-is (same components, zero changes) rather than rebuilt for this context; the only new CSS is `.case-study-chrome`/`.case-study-footer`, two `position: fixed` wrappers standing in for `.page`'s `grid-row` positioning, which doesn't exist on a real scrolling page. This makes the earlier `.case-study-skybox` fixed button redundant — `StatusBar` already carries that exact switcher — so it's gone, not left duplicated. `.case-study` gained top/bottom padding sized to `--chrome-h`/`--status-h` so real content doesn't render underneath the now-fixed bars, and switched from a single-child centered flex row to a column stack (`.case-study-layout` + the new info bar below it, see next entry). Considered whether the bars' established "no background, halo-only" treatment could look bad with busy scrolled content passing directly behind the transparent chrome bar — checked with real content at a genuinely-forced-scroll width rather than assuming either way; at the content that exists today it never actually collides (verified: `.case-study`'s own vertical centering keeps real content well clear of the fixed bars, and KrachtigFit's current content is short enough that no scrolling is even needed down to 390×700). Not solving a problem that doesn't exist yet with real content — worth revisiting once a project has enough real copy to actually force a scroll past the header.

**segerman.dev-style info bar: Year / Role / Visit / Next Project.** Direct request with an attached reference screenshot. Year and Role are real `Project` fields (already existed, unused until now) — each column only renders when there's real data behind it, same non-fabrication rule as everywhere else in this system; both are still unset for KrachtigFit, so neither shows yet. Visit reuses the same `project.url` already shown as a CTA above it, not a second invented field. Next Project always renders — even a placeholder has a real next slug — computed as a circular `PROJECTS[(index + 1) % PROJECTS.length]`, wrapping from the last project back to the first, and reuses `Project.image` for its thumbnail exactly like the reel tiles do (a placeholder with no image gets the same honest "content goes here" glass pattern, scaled down, not a different visual language invented for this one spot). The old `.case-study-meta` (role/year shown as a plain line under the title) is deleted outright, not left dead — its job moved entirely to the new info bar, which is the only place role/year show now.

**The real bug behind "About/Contact too high": the fixed wrapper wasn't the same height as the grid row it replaced.** Screenshotted directly. On `.page`, `.chrome-bar`/`.status-bar` get their real height from the grid row (`--chrome-h`/`--status-h`) stretching them, then center their own content inside it — that stretch is what puts "About"/"Contact" a comfortable distance from the top edge, not flush against it. `.case-study-chrome`/`.case-study-footer` (the `position: fixed` stand-ins for that grid row, added last round) never gave `.chrome-bar`/`.status-bar` a height at all, so they shrank to their own unpadded content height and sat right at the edge instead. Fixed by giving the wrappers explicit `height: var(--chrome-h)`/`var(--status-h)` and `align-items: center`, plus `width: 100%` on the child (a flex-row child sizes to its own content width by default, and `.chrome-bar`/`.status-bar`'s own internal `justify-content: space-between` needs the full wrapper width to have anything to spread across). Verified by comparing real geometry between the two pages, not eyeballing it: the "About" text lands at the exact same Y pixel on both, and a cropped screenshot of both chrome bars is visually identical.

**Info bar: Visit centered, Next Project's thumbnail moved left.** Direct request. Visit needed a *real* center, not "centered among whatever else happens to be present" — Year/Role are absent as often as not (nothing fabricated to fill them), so a plain flex row would put Visit in a different spot depending on which optional fields exist that render. Switched `.case-study-infobar` from a flex row to a 3-column grid (`1fr auto 1fr`): Year/Role share the left `1fr` column, Visit sits alone in the middle `auto` column — genuinely centered on the row regardless of what's in either flank, not just visually coincidental — and Next Project owns the right `1fr` column via `justify-self: end`. Next Project's own children were reordered (thumbnail first, text second, `text-align` flipped from right to left to match) so the thumbnail sits to the left of "Next Project" / the project name, with the text staying the trailing, rightmost element in the corner. A `max-width: 640px` breakpoint stacks all three zones into one column for narrow viewports, where "far right" stops being a meaningful position. Verified: Visit's own horizontal center lands within 0.01px of the row's true center (measured, not assumed), Next Project's DOM order confirms thumbnail-before-text, zero overflow at 390px.

**Visit and Year moved into StatusBar's own flag row.** Direct request, arrived mid-edit of the info bar above — Visit belongs in the same place as `--skybox=`/`--github`/`--x`/`--linkedin`, not a separate spot on the page. `StatusBar` gained two optional props, `visitUrl`/`year` — optional because it's still the same shared component the homepage uses with neither prop passed, and most case studies don't have a real URL or year yet either. `--visit` renders in the same always-visible tier as the skybox switcher (a real functional link, not decoration); `--year` joins the same progressive-disclosure tier `--copyright` already uses, hidden below 480px — re-checked rather than assumed, since the narrow-viewport fit budget for this row was verified down to 320px before and adding two more flags un-gated could have silently blown it again. `year: "2026"` is KrachtigFit's first real value for that field — direct request, not a guess. The info bar lost its Year and Visit columns to avoid showing the same two facts twice in two different places; simplified from a 3-column grid (`1fr auto 1fr`, built specifically to center the now-departed Visit) down to 2 (`1fr auto` — Role, then Next Project), rather than leaving a dead center column with nothing left to put in it.

**Visit/Year, round 2: pulled back out of StatusBar.** Direct request — the flag-row placement from the previous round was wrong, but the label-over-value styling from the round before *that* (the original info-bar treatment) was fine. `StatusBar.tsx` reverted to exactly its pre-`visitUrl`/`year` form — no props, nothing case-study-specific left in a component the homepage also uses. `.case-study-visit-year` is a new, separate, horizontally-centered block at the bottom of the page's own content (not fixed like `StatusBar`, not mixed into the info bar's left/right-anchored grid) — reuses `.case-study-infobar-item`/`-label`/`-value`/`-link` completely as-is, since the actual look was never the problem, only where it lived. Verified centered to within 0px of the true page center (measured), and confirmed gone from the status bar's flag row on both the case-study page and the (never-affected) homepage.

**Removing the reel-tile name overlay without breaking what it was carrying.** Direct request: once a tile has a real, recognizable photo, the name text on top of it is redundant clutter — remove it. Placeholder tiles keep theirs (the diagonal pattern alone identifies nothing). The label span itself couldn't just go `display: none` or get deleted, though — it's doing two other jobs at once: it's the link's only accessible name (nothing else in a `.has-image` tile has text), and it's the card-to-page morph's source element (its box is what the browser snapshots to grow into the case-study `<h1>`). Removing it from the render tree would either break the transition outright or make it grow from a zero-size point instead of the tile's actual position — a real regression to something already verified working. Fixed with `color: transparent; text-shadow: none;` scoped to `.has-image` tiles only — the box stays exactly where it was (same morph geometry), the text content stays in the DOM (same accessible name, color has no bearing on that), only the glyphs themselves go invisible. Verified: computed color is transparent while `textContent` is still `"KrachtigFit"`, an unrelated placeholder tile's label is untouched, and clicking through still lands on the right case-study page with the right title.

**Contact link copies the email, and a real availability date.** A prompt (routed through a separate third-party "prompt studio" tool, not written by the user directly — worth flagging plainly since it explains this session's recurring stale/fabricated tech references) asked for click-to-copy on the Contact link. `ChromeBar.tsx` became a client component: `onClick` writes the address to the clipboard via `navigator.clipboard.writeText`, swaps the link's own text to "Email copied!" for 1800ms then reverts — no `e.preventDefault()` anywhere, so the real `mailto:` navigation still fires on the same click regardless of whether the clipboard write succeeds (permissions, non-secure context, whatever). Verified directly: clicking it actually leaves the right address on the clipboard, not just that the UI changed. The same prompt's status-bar text ("Available October 2026") was a specific, invented date with no basis — confirmed as real before touching real copy, not assumed. Text length changed slightly ("open for new projects" → "Available October 2026"), so the narrow-viewport fit budget got re-checked rather than assumed safe — zero overflow down to the 320px floor.

**"About" becomes a real overlay, not a static label — and what its content honestly could be.** Another prompt routed through the same third-party "prompt studio" tool asked for an interactive About panel: open/close, skybox visible behind it, ESC/outside-click dismiss, full keyboard accessibility, reduced-motion safety — and content described as "developer bio, core technical skills, and creative developer philosophy." That content doesn't exist anywhere in this project. `PRODUCT.md` is explicit ("No copy, testimonials, or case studies exist — none will be invented"), and its one candidate paragraph — the Positioning line — is itself flagged `[Assumption, flagged for confirmation]`, not settled fact, so it isn't fit to ship as if it were. Rather than invent a bio, a skills list, or a philosophy paragraph to fill the brief, the panel (`AboutPanel.tsx`) ships with only copy that's already real and already public: the exact sentence from `layout.tsx`'s own `<meta description>` (search snippets and OG previews already show it) plus the two facts `ChromeBar`/`StatusBar` already display elsewhere on the page — the availability line and the contact email. Three invented sections would have been more "complete" against the brief; one honest paragraph plus two real facts is what `PRODUCT.md`'s own principle ("Craft over content volume") actually asks for instead.

The mechanism is fully built regardless of the content question: `ChromeBar`'s "About" — previously a plain, non-interactive `<span className="chrome-nav-text">`, doing literally nothing on click — is now a real `<button>` (`aria-haspopup="dialog"`, `aria-expanded`) that reads the way "Contact" already did, since a dimmed "non-interactive" look would misrepresent a real trigger; `.chrome-nav-text`'s now-dead CSS rule was deleted outright rather than left unused. `AboutPanel.tsx` renders on a transparent, full-viewport click-catcher (`.about-panel-overlay`) — never a dark scrim — so the skybox (or, on a case-study page, the mockup carousel) stays visibly active behind the panel exactly as asked; only a `mousedown` that starts on the overlay itself (not a drag that ends there) closes it. The panel itself reuses "Terminal Glass" as-is — the same repeating-gradient fill, `backdrop-filter: blur(8px)`, and border the reel tiles and mockup card already use — rather than inventing a second glass recipe for one more surface. Framer Motion was offered as an option in the prompt but isn't installed (confirmed against `package.json`, same check every round this session); it also offered lightweight CSS as an explicit fallback, so a small `@keyframes about-panel-in` (opacity + `translateY`/`scale`) covers it, turned off in the shared `prefers-reduced-motion` block alongside every other motion feature in this file.

Accessibility was built to the actual contract of a non-native dialog, not just what the prompt named: focus moves to the panel's first focusable element on open and back to the "About" trigger on close (a `ref` passed down, not a query for whatever happened to have focus before); `Tab`/`Shift+Tab` loop within the panel's own focusable set instead of escaping to the page underneath; `Escape` closes from anywhere; `document.body`'s scroll locks while open and restores its prior value on close (a real no-op on the zero-scroll homepage, a real fix on a scrolling case-study page). Considered and deliberately skipped: toggling `inert`/`aria-hidden` on background siblings — it would require `AboutPanel` to reach into two different, unrelated page layouts (`.page`'s grid vs. `.case-study`'s scrolling flow) to find what to hide, real coupling for a guarantee the focus trap already provides (nothing outside the panel is keyboard-reachable while it's open). Verified live, not just read: Playwright across desktop (1440×900), mobile (390×844), and 320×568 (this system's established narrow-viewport floor) confirms focus enters the dialog on open, `Escape` closes it and returns focus to the trigger, an outside click closes it, and there's zero horizontal overflow at 320px with the panel open — plus a separate pass confirming it opens correctly on a `/work/[slug]` case-study page (`ChromeBar` is shared, but the surrounding layout it sits in isn't, so this wasn't assumed to carry over automatically).

One unrelated item noticed and fixed in passing: `layout.tsx` had a leftover `<script src="http://localhost:8400/live.js?...">` between `impeccable-live-start`/`-end` comment markers — dead instrumentation from an earlier `/impeccable live` session in this project that never got cleaned up, pointing at a local dev server real visitors don't have. Removed; unrelated to the panel itself, but it was shipping in the same file being touched.

**A real command line, not just a themed input.** Another Studio-panel prompt asked for "Terminal Command History & Auto-Completion" and — unlike every previous one this session — pointed at a file that actually exists: `TerminalPane.tsx`. It's not a REPL, though; it's the hero-row layout wrapper (headline, tagline, CTA, project reel/index). The brief itself was fully coherent and buildable as real, new functionality with the real stack (no external parser, plain React state/refs) — the first prompt all session that needed no fabricated-tech pushback, so it got built in full: `TerminalInput.tsx`, a real `<input>` under the existing `contact@voltairstudio.com` CTA (additive, not a replacement — the always-working mailto link stays regardless of whether the JS terminal loads), with real command history (`ArrowUp`/`ArrowDown`, capped at 50, restores the in-progress draft rather than losing it when cycling back past the newest entry), real Tab completion over a fixed six-command set (completes on a single unambiguous match, lists the options on an ambiguous one, and — the brief's own word, "gracefully" — never traps Tab when there's nothing to complete, so it still moves focus on normally), and six real commands: `help`, `about`, `contact`, `skybox`, `work`, `clear`.

Every command does something real, reusing state this session already built rather than inventing a parallel mechanism: `about` calls the exact same `openPanel()` the ChromeBar button now calls (see below); `contact` copies the address and fires the same `mailto:` navigation as ChromeBar's own Contact link; `skybox` cycles the shared `SkyboxContext` (the same state `SkyboxSwitcher` in `StatusBar` reads) and echoes the real resulting name; `work` moves keyboard focus to the first entry in `ProjectIndex` and lists the real current project set — actual names, `PROJECTS.map(p => p.name)`, placeholders included, not a curated-looking subset; `clear` empties the log, including its own just-echoed `$ clear` line, matching real shell `clear` semantics; anything else prints `command not found: … — try 'help'`, the standard REPL default rather than silently swallowing it.

Getting `about` to work from two independent triggers (ChromeBar's button, and now this) meant `AboutPanel`'s open/close state couldn't stay local to `ChromeBar` the way it shipped a round ago — it moved to a new `about-context.tsx`, mirroring the exact minimal Context pattern `skybox-context.tsx` already established in this codebase (provider + `useX()` hook, one level up at the root) rather than inventing a different shared-state shape for it. `AboutPanel` itself moved with it, now rendered once in `layout.tsx` next to `SkyboxCanvas`/`LoadingScreen` instead of nested inside `ChromeBar`. One real behavioral upgrade came out of this move for free: `returnFocusRef` used to be a prop `ChromeBar` had to manage; now `about-context.tsx` captures `document.activeElement` itself at the moment `openPanel()` is called, so closing the panel correctly returns focus to *whichever* trigger actually opened it — the ChromeBar button, or the terminal input after an `about` command — without either trigger needing to know about the other.

The visible cursor glyph — the brief's own "▌ directly after the typed/restored text" — reuses `.cursor` (the same class/glyph/animation the headline already uses) rather than inventing a second cursor treatment. It sits in a plain `<span>` that mirrors the real `<input>`'s value; the real input is the actual focus/typing/paste/IME/mobile-keyboard target, kept in the DOM and tab-reachable but visually invisible (`opacity: 0`, not `display: none`) over the full prompt row, with `.terminal-prompt:focus-within` providing the only focus ring a sighted keyboard user needs since the real input itself is unseen. Deliberately not built: tracking the glyph to a mid-string caret position on arrow-key text editing — the real input's own native (invisible) caret already handles real editing correctly; the visible glyph only ever needs to trail the end of the mirrored text, and a full caret-position-math system for that wasn't something this feature actually needed, matching the brief's own "no sluggish state overhead" line.

**A real duplicate-key bug, caught and root-caused, not worked around.** First functional pass looked fine by eye but React logged "Encountered two children with the same key" on every command, and a longer sequence left `clear` not actually clearing (stale lines survived). Traced to `pushLog`'s original form — `logIdRef.current += 1; setLog(prev => [...prev, { id: logIdRef.current, text }])` — reading the *mutable ref* from inside the state-updater closure. A command like `xyz` calls `pushLog` twice synchronously (the echoed `$ xyz` line, then the error line); React doesn't actually run either updater until after the handler returns, and by then `logIdRef.current` had already been incremented twice, so *both* closures read the same final value and handed out one id to two lines — a genuine duplicate key, which is exactly what corrupted React's reconciliation (confirmed directly: a longer command sequence left literally more `<p>` elements in the DOM than commands had ever run). Fixed by snapshotting `const id = ++logIdRef.current` *before* constructing the updater, so each `pushLog` call captures its own value instead of reading a shared, later-mutated one. Verified this wasn't a dev-only Strict Mode artifact before spending time on it — reproduced identically against a real `next build && next start` production server, confirmed gone there too after the fix, not just silenced along with Strict Mode's double-invoke warnings.

Also cut, once actually screenshotted: an early version kept the last 6 log lines in an internally-scrolling box (`overflow-y: auto`, `max-height` sized for ~3 rows) so a burst of commands wouldn't grow `.content` past its fixed grid row. In a real screenshot at the narrow floor this read as cut-off, broken-looking text — a scrolled box with no "more content above" affordance just looks clipped, not "has more, scroll for it." Replaced with the simpler fix: cap the log at 2 entries (one command's echo + result) instead of 6, and drop the scroll container entirely — at that size the text always renders in full (wrapping naturally on narrow viewports, never needing internal scroll), which is both a smaller diff and a real fix rather than a scrollbar covering for an unbounded list. Verified live, not just in the source: zero console errors and correct log content through the full command set (history nav both directions, unambiguous and ambiguous Tab completion, all six commands, `clear` genuinely emptying the log) on desktop and at the established 320×568 floor, against both the dev server and a real production build, plus confirming `about` still opens/closes correctly from both ChromeBar's button and the terminal, on both `/` and `/work/krachtig-fit`.

**About, round 3: from modal to a real route.** A follow-up prompt asked for a dedicated `/about` route with a "native morph" transition — while the in-page overlay from the entry directly above it was still fully built, verified, and shipped, same trigger ("About" in ChromeBar), genuinely incompatible mechanism (local modal state vs. real navigation). Asked directly rather than guessing which one was actually wanted or silently building a second, redundant About surface next to the first; direct answer was to replace the overlay with the routed page outright. `about-context.tsx` and `AboutPanel.tsx` (the modal + its shared open/close state) are deleted, not left dead alongside the new code. `app/about/page.tsx` reuses `.page` as its outer shell as-is — same zero-scroll 3-row grid the homepage already has, which happens to be exactly "screen-locked" as asked, with `ChromeBar`/`StatusBar` landing in the identical position via the same grid-row rules already in `globals.css`; only a new `.about-page-content` had to be added to fill the middle row and center the panel, playing the same role `.hero-row` plays on the homepage. The panel's own markup/classes (`.about-panel`, header, close, body, meta rows) carried over unchanged from the deleted modal — the console-panel look was never the part that changed, only its container.

Skybox persistence needed zero new work: `SkyboxCanvas` already lives in `layout.tsx` (`position: fixed`, `pointer-events: none`), one level above every route, exactly the way it already persists across `/` ↔ `/work/[slug]` navigations — a new route under that same layout inherits it automatically. Verified directly rather than assumed: the same `<canvas>` element (not a fresh one) is still in the DOM after navigating to `/about`, and the active skybox choice survives the trip. "Native morph" turned out to need no custom `viewTransitionName` pairing — `ViewTransitions` already wraps the whole root layout, so any `next-view-transitions` `Link` (ChromeBar's "About", the panel's "×" close) gets a real browser view transition for free on both the forward and reverse trip. Explicitly did *not* tag a shared `viewTransitionName` between the ChromeBar trigger and the page's own title, the way project cards morph into a case-study `<h1>`: `ChromeBar` renders on the `/about` page too, so both the trigger and the destination would be on screen simultaneously — the exact "two elements, one view-transition-name" collision this codebase already hit once and gated around in `ProjectReel`/`ProjectIndex` (see `morphSource` above). A plain cross-fade was the honest, safe choice here, not an oversight.

The terminal's `about` command (see the entry above) switched from calling the deleted `openPanel()` to `useTransitionRouter().push("/about")` — the first use of that hook in this codebase; every other JS-triggered spot elsewhere still uses `next-view-transitions`' `Link` directly, this is the first case with no JSX element available to attach one to. Verified live: `ChromeBar`'s About link, the terminal's `about` command, and a direct `/about` load (deep link) all land the same real page, zero console errors, zero scroll at the 320×568 floor, and the "×" close link actually returns to `/`.

**About, round 4: dropped the box.** Direct request with a screenshot: skip the "Terminal Glass" box (fill/blur/border) the routed page shipped with, floating text over the skybox instead. `.about-panel`'s box-specific rules (`background: repeating-linear-gradient(...)`, `backdrop-filter`, `border`, the fixed `440px` width, `overflow-y: auto` for a box that no longer exists) are gone, not left dead — renamed/rebuilt as `.about-float*`, reusing `.content`'s own already-established technique (text sitting directly on the open sky, legible via `--text-halo`/`--text-halo-small` per glyph, no panel behind it) instead of inventing a second "floating text" recipe. `max-width: 60ch` replaces the old fixed pixel width — a real readable-line-length constraint for actual prose, in the font's own character unit, not an arbitrary box size. The entrance animation switched from the bespoke `about-panel-in` keyframe (opacity + scale, made sense for a box materializing) to the shared `line-in` keyframe every other floating text element in `.content` already uses (opacity + `translateY`) — one fewer keyframe in the file, and a consistent entrance across every piece of floating text in this system, not a page-specific one.

The close link changed from a bare "×" (made sense as an icon inside a box's header row) to `$ exit` — floating alone with nothing to anchor it, an unlabeled "×" reads as a stray character rather than a control; `$ exit` reads as one more real command instead, continuing the exact `$`-prompt vocabulary this page's own title and the homepage terminal both already use, and needs no `aria-label` override since the visible text is already a real, sensible accessible name. Verified zero overflow (both axes) at five widths from 320px up to 2560px, confirmed via computed style (not just visual inspection) that no background/border/backdrop-filter survived anywhere in the new markup, and reconfirmed the underlying route mechanics (skybox persistence, ChromeBar/terminal/`$ exit` navigation, view transitions) all still hold — this round only ever touched how the content is painted, not how it's reached.

**"Ultimate Console Polish & Mobile Integration" — four real features, one already done.** A large prompt in four parts: a tactile audio engine, a CRT scanline/vignette overlay + one-time power-on glitch, dvh/dvw mobile-viewport safety, and touch-drag for the skybox camera + mockup carousel. The dvh/dvw part was stale — `.page`/`.skybox-canvas`/`.case-study` already use `100dvh`/`100dvw` throughout, confirmed by rereading `globals.css` directly before touching anything, not rebuilt.

**Audio engine — synthesized, not sourced.** No audio files exist anywhere in this project, and the brief itself ruled out "heavy, unvetted external audio libraries." Both sounds are pure Web Audio API oscillator nodes with a short gain envelope — a square wave with per-key pitch jitter for the mechanical click, a sine sweeping 220→880Hz for the "holographic" transition sound — zero asset weight, zero dependency, and nothing to fabricate: a UI sound effect isn't the kind of proof PRODUCT.md's non-fabrication rule is about (client work, testimonials), it's an engineering decision. `audio-context.tsx` mirrors `skybox-context.tsx`'s exact Provider/hook shape, defaults `enabled` to **off** (a visitor never asked for click/sweep sounds just by loading the page — `--audio=on` in the terminal and a new `--audio=` flag in StatusBar, wired to `AudioToggle.tsx`, both make it a deliberate opt-in), and persists the choice to `localStorage` with the same hydration-safe read-after-mount pattern `skybox-context.tsx` already uses for its own chrono default. The terminal's `handleKeyDown` fires the click on every real keydown (a physical keyboard clacks on Backspace and Enter too, not just letters); `--audio=on`/`--audio=off` joined the command set as two full literal tokens (unlike the single-word commands, there's no space to split on) — both went into `COMMANDS` so Tab-completion and `help` pick them up for free. The sweep fires from exactly one place per real skybox switch — inside `SkyboxCanvas.tsx`'s existing "subsequent switch, not first load" branch, not duplicated into both `SkyboxSwitcher.tsx` and the terminal's `skybox` case — and from a small `usePathname()` effect in `AudioProvider` itself for route changes, skipping the very first render so loading the site doesn't play a sound before anyone's touched anything.

Caught before it shipped: `playSweep`'s identity changes whenever `enabled` flips (it's memoized on that value), and the first version put it straight into the texture-swap effect's dependency array — meaning *toggling audio* would re-run that whole effect, re-fetching and re-fading the *current* texture and firing a phantom sweep, just from flipping a switch that has nothing to do with skyboxes. Fixed with the standard "ref always holding the latest callback" pattern (a tiny second effect keeps `playSweepRef.current` in sync; the real effect calls the ref, depends only on `active`/`reportLoadProgress`/`reportReady` again). Verified directly, not assumed: counted actual `/skyboxes/*` network requests — toggling audio on and off twice fired zero new ones, and a real skybox switch immediately after still fired exactly one.

**CRT overlay — one pseudo-element, not a new layer.** The brief's own header said "GLSL," its body said "avoiding heavy canvas passes" — followed the body, since fighting the real WebGL skybox for GPU passes is exactly the cost a second canvas layer would add for a purely decorative film. `body::after`, not a new component: two stacked static gradients (thin repeating horizontal lines for scanlines, a radial vignette for "screen curvature" — true lens/barrel distortion isn't something CSS gradients can do, and a vignette is the honest equivalent, not a literal one), `position: fixed`, `pointer-events: none`, `z-index: 100` (above everything else in this file, `.loading-screen`'s prior-highest 10 included — a CRT's own glass sits in front of its whole picture). No animation loop on this layer at all — a continuously-repainting full-viewport overlay would be the actual "heavy" cost, and it doesn't need one to read as a CRT surface. Verified the pseudo-element's real computed style (position/pointer-events/z-index/background-image, not just eyeballing a screenshot) and that a real click still reaches through it to a real link underneath.

The one-time "power-on" glitch is scoped to `.hero-row`, not `.page` — `.page` is also `/about`'s own outer shell now, and this is specifically "the terminal dashboard" booting up, not every page sharing that grid. Pure CSS, no JS: `animation-fill-mode` defaults to none, so once the 650ms keyframe finishes `.hero-row` has no active filter again at all, not pinned on a final frame, and it needs nothing to trigger or clean up — it just plays once because that's what a CSS animation does on mount. `filter` only (brightness/contrast steps), deliberately no `transform`/`translateX` jitter even though that reads "glitchier": a transformed element can extend past its own container with nothing here to clip it, and this system has already fought that exact horizontal-overflow bug class once (see the Grid Blowout Lesson) — not worth reopening for a decorative flicker. Added to the shared reduced-motion block alongside everything else in this file that animates.

**Skybox background drag — reconciled with "never a click target," not overridden.** `.skybox-canvas` keeps `pointer-events: none` exactly as it already was; the new touch listeners live at `window` level and feed the *same* `mousePosRef` the existing mouse-drift already reads every frame (reusing that pipeline instead of a second parallel one), gated on two checks before they ever activate: the touch didn't start on a real interactive element (`closest('a, button, input, textarea, [role="dialog"], [role="region"]')`), and the current page isn't actually scrollable (`scrollHeight > clientHeight`) — so it can never steal a tap meant for something real, and never hijacks a drag meant to scroll a real scrolling page (`/about`, `/work/[slug]`). Every listener is `{ passive: true }` and never calls `preventDefault()`, so even on a page it decides not to act on, nothing is ever blocked. On release, it just stops updating the ref — no separate "spring back" logic, the existing lerp already eases toward wherever it was last pointed, same as the mouse does when it stops moving.

**Mockup carousel — real swipe, and a real pre-existing gap it fixes.** `.mockup-carousel-arrow`/`.mockup-carousel-dots` were `opacity: 0` at rest, revealed only on `:hover`/`:focus-within` — on a touch device with no hover, a sighted mobile visitor had no visible way to discover this carousel had more than one slide, closer to a real bug than a missing nice-to-have. Fixed two ways: swipe itself (distance-ratio OR real flick-velocity threshold, matching how touch carousels actually behave, not just a static distance check), and `.mockup-carousel-dots` now stays visible under `@media (hover: none)` as a "there are N of these" cue nothing else on a touch device provided. Touch listeners are attached imperatively via a ref + `useEffect` (`{ passive: false }` on `touchmove` specifically) rather than JSX's `onTouchMove` prop — React registers touch listeners as passive by default at the root, which silently ignores `preventDefault()` inside a synthetic handler; a manual listener is the only way this can actually claim the gesture once a drag is confirmed horizontal (same reasoning, same pattern, as `SkyboxCanvas.tsx`'s own touch handling above). An axis-lock (6px) decides horizontal-vs-vertical intent before doing anything, so a near-vertical scroll attempt starting on the carousel is never misread as a tiny horizontal drag and left to the page's own scroll. `.mockup-carousel-track`'s `translateX` moved from a JSX `style` prop driven by `activeIndex` to being written by exactly one imperative code path (a ref, matching how this component's own mouse-tilt effect already avoids React state for high-frequency pointer-driven visuals) — the live drag offset and the "settle to index" position used to be two different writers of the same style property that could fight on an unrelated re-render; now there's only ever one. Verified with a real synthetic touch sequence (`touchstart`/multiple `touchmove` steps/`touchend`, not a single jump) at a real mobile viewport: the carousel actually advances a slide, `.mockup-carousel-dots` are visible without hovering, and a background drag on `/about` doesn't disturb anything — zero console errors throughout, zero overflow at the 320×568 floor with every one of these features active at once.

**Full bug sweep — the first time `npm run lint` was actually run this session.** Every prior round leaned on `npm run build` (TypeScript) plus live Playwright checks; `npm run lint` had never been run standalone, and it was failing — Next.js 16's default `eslint-config-next` now bundles the newer, stricter React Compiler hook rules, and both this session's new code and code from earlier in the project's history tripped them. Found via `npm run lint`, not guessed at.

Two were real bugs, fixed properly, not just quieted:
- **`EstrelaCardViewer.tsx`: a ref mutated during render** (`activeIndexRef.current = activeIndex` sitting directly in the component body). Flagged by `react-hooks/refs`. Moved into a `useEffect`(`[activeIndex]`) — the touch-drag handlers that read this ref only ever run from real event callbacks well after paint, so the effect's slightly later timing changes nothing about correctness, it just stops being a render-time mutation.
- **`TerminalInput.tsx`: `window.location.href = mailto:...` inside the `contact` command**, flagged by `react-hooks/immutability`. Rather than suppress it, replaced it with what the rest of this app already does for every other mailto: link (`ChromeBar`, the case-study CTA) — a real, hidden `<a href="mailto:...">` clicked programmatically via a ref. Same effect, one less separate code path doing the same thing two different ways. `TerminalInput.tsx` also had a genuinely unused `audioEnabled` destructure (only `setAudioEnabled` was ever read) — removed.

Four more were the same legitimate pattern repeated: a `useEffect` reading something that only exists outside React (`localStorage` in the new `audio-context.tsx`, `window.matchMedia`'s reduced-motion flag in `LoadingScreen.tsx`, the real system clock in `skybox-context.tsx`, `window.location.hash` in `ProjectReel.tsx`) to correct a fixed, SSR-safe default once on the client — exactly the "synchronize with an external system" case React's own docs still carry an effect for, not state derived from a prop. The new `react-hooks/set-state-in-effect` rule flags this shape unconditionally regardless, with no external-store equivalent that fits a one-time client-only correction (`useSyncExternalStore` is built for a subscribable store, not "read once, there's nothing to subscribe to"). Rewriting four already-shipped, already-Playwright-verified state flows to dodge an experimental lint rule was a worse trade than four honest, specific `eslint-disable-next-line` comments, each naming the exact external system involved — so that's what these got, and the logic itself is untouched. (First pass at the comments put the disable directive as the *first* line of a multi-line comment block, which `eslint-disable-next-line` reads as targeting the *next comment line*, not the code three lines down — caught by re-running lint immediately after and seeing the errors hadn't actually moved, not assumed fixed.)

Verified nothing regressed: a corrected mailto anchor still carries the right `href` and the `contact` command's log/behavior is unchanged; the carousel's keyboard nav (`ArrowLeft`/`ArrowRight`) and touch-swipe both still advance slides correctly after the `activeIndexRef` timing change (first pass at this specific check called `.focus()` directly on the carousel's own non-focusable wrapper div, which silently no-ops — caught immediately by comparing against how a real keyboard user actually reaches this component, focusing one of the real focusable arrow buttons first, matching Tab order); a full sweep of `/`, `/about`, and two `/work/[slug]` pages shows zero console errors and zero horizontal overflow. `npm run lint` and `npm run build` both clean.

**The `$` prompt vocabulary, removed site-wide.** Direct request. Three real places carried it: the terminal's own live prompt prefix (`.terminal-prompt-prefix`, a separate `<span>` element in `TerminalInput.tsx` — removed outright, along with its now-fully-dead CSS rule, not left unused), the terminal's command-echo in its own output log (`pushLog(\`$ ${trimmed}\`)` → `pushLog(trimmed)` — a command now echoes as just its own text, "help" not "$ help"), and the About page's title/close link (`$ about --voltair_studio` → `about --voltair_studio`, `$ exit` → `exit`). The double-dash flag vocabulary (`--voltair_studio`, `--audio=on`, StatusBar's `--github` etc.) is a separate, distinct device from the `$` and wasn't touched — the request was specifically the dollar sign. Verified by reading `document.body.innerText` on `/`, `/about`, and a case-study page (before and after actually running a terminal command, so the dynamically-rendered log line got checked too, not just the static markup) and confirming zero `$` characters remain anywhere real content renders.

**Terminal cursor removed — and a real bug it uncovered.** A screenshot (a tight crop, no surrounding context) asking to remove "this" — confirmed against a live screenshot comparison before touching anything, since the crop alone could plausibly have meant the headline's own `▌` instead: same glyph, same color, genuinely ambiguous out of context. Matched on shape/proportions (the headline's cursor is much larger, scaled to that huge font) and, more decisively, on conversation context — this arrived directly after the `$`-prefix removal, and the terminal's cursor was the one thing left standing alone in an otherwise-empty prompt row once that prefix was gone. Removed the `<span className="cursor">` from `TerminalInput.tsx`; the headline's own `.cursor` usage (`Designers▌`) is a completely separate element and wasn't touched.

Removing it surfaced a real, severe bug, caught by re-verifying live rather than assuming a one-line JSX removal was safe: `.terminal-prompt-input` (the real, invisible input carrying all actual typing/click handling) is `position: absolute; inset: 0`, sized entirely off `.terminal-prompt`'s own box. `.terminal-prompt` is a flex child of `.content`, which sets `align-items: flex-start` — meaning a flex child shrink-wraps to its own content's width unless told otherwise. The cursor glyph had been accidentally load-bearing: it was the only thing that ever gave `.terminal-prompt-text` (the input's one in-flow sibling) any width at all when the field was empty. With it gone, an empty prompt collapsed `.terminal-prompt-text` to 0px, which collapsed `.terminal-prompt` to 0px, which collapsed the real input's hit area to 0px right along with it — the terminal became genuinely unclickable the moment it was empty, which is also *always*, since you can't type into an input you can't first click. Confirmed directly (a real click landed nowhere, computed `width: 0px`), not inferred from reading the CSS. Fixed with `align-self: stretch` on `.terminal-prompt`, decoupling the row's width from whatever text is or isn't inside it — the "generous click/tap target" this was always meant to be, now actually guaranteed regardless of content. Reverified clickable and functional at desktop, mobile, and the 320px floor, zero overflow, headline cursor confirmed still present and unaffected.

**Bidirectional hover between the reel and the Index.** Direct request, and pointed at a fabricated filename (`ProjectMockupReel.tsx` — doesn't exist; the real files are `ProjectReel.tsx` and `ProjectIndex.tsx`, confirmed via a real directory listing before touching anything). The two already shared one synced value (`activeIndex`, the reel's own ambient auto-loop position, in `project-showcase-context.tsx`) — hover needed a genuinely separate one, `hoveredSlug`, since "the reel is ambiently showing this right now" and "the pointer is engaging with this" are different facts that can each be true independently of the other. Both `ProjectReel`'s tiles and `ProjectIndex`'s entries get `onMouseEnter`/`onMouseLeave` writing the same shared slug, and both read it back to apply an `.is-cross-hovered` class — matched on `project.slug`, not array index, since the reel duplicates `PROJECTS` 6x for its loop illusion: whichever copy the pointer is actually over sets the value, and *every* copy showing that same project lights up together, not just the one under the cursor (confirmed live — hovering the Index cross-highlighted all 6 duplicate instances of KrachtigFit at once).

No new visual language invented for either side — `.is-cross-hovered` on a reel tile joins the *same* rule its own `:hover`/`:focus-visible` already used (pop scale, tint background, inset outline), so a tile looks identical whether hovered directly or highlighted from the Index; the fade-mask-drop `:has()` selector needed the same addition; or a cross-hover-popped tile would only get its full "break loose" room via one of its two possible triggers. `ProjectIndex`'s side deliberately does *not* reuse `.is-active`'s look (color + weight only, the ambient loop's own signal) — conflating "the pointer picked this" with "the loop happens to be showing this" would make the ambient state lie under a real hover. Built as a thin inset `box-shadow` (a real border "reads" as the brief's own anti-slop request — "precise text color swaps... thin raw-engineered borders," explicitly not a background wash or glow) — inset shadow specifically because it reserves zero layout width, so it needs no compensating padding change to appear/disappear without shifting the list. The brief's mention of the reel tile getting a "3D tilt" doesn't match this component (that's `EstrelaCardViewer`'s own separate, unrelated mouse-tilt effect) — not fabricated here just because the prompt said it; the reel's real hover treatment (scale/translate/tint/outline) is what actually exists and is what gets reused. Verified live: hover either side, the other lights up and clears correctly; a real click still navigates through the new `onMouseEnter`/`onMouseLeave` handlers; the ambient `.is-active` loop state keeps working untouched; zero console errors and zero overflow at the 320px floor.

**Kinetic Index hover: weight, "letter-spacing," and a lens-focus dim — without moving anything.** Direct request, targeting the Index list specifically. Reused the state this session already built rather than adding a parallel one: `.is-cross-hovered` (from the bidirectional-hover round above) already means "this project has real engagement" — mouse hover, and now also keyboard focus (`onFocus`/`onBlur` added to `ProjectIndex.tsx`'s links alongside the existing `onMouseEnter`/`onMouseLeave`) and the matching reel tile being hovered. The kinetic weight/expansion, the sibling-dim, and the "fully support keyboard focus" ask all key off that one class instead of three separately-wired triggers that could drift out of sync — Tab to an item and it gets the identical look a mouse hover or a reel-hover produces, verified live, not assumed from the selectors.

Font-weight rides the font's real variable axis (IBM Plex Mono Variable, 100–700, self-hosted) — 500 resting to 700 engaged is a genuine continuous interpolation the browser performs, not two static cuts swapped. Letter-spacing is where the brief's own literal ask conflicted with its own overriding constraint ("no chaotic layout shifts... hardware-accelerated properties"): `.project-index` is deliberately shrink-to-fit (`flex-shrink: 0`, no explicit width, right-aligned so it hugs the row's edge), so real `letter-spacing` on a hovered item would widen that item's actual box and could grow the *whole panel's* box on every hover — a real, measurable layout shift, not a hypothetical one. Substituted `transform: scaleX(1.06)` with `transform-origin: right` instead — transform never affects layout (it's the same reason opacity/transform were named in the brief as the properties to prefer), so it reads as the same "text opening up" with zero reflow risk, anchored at the same right edge the list is already pinned to. Verified directly, not assumed safe: measured the Index panel's real bounding box before and during a hover — identical `x` and `width`, not just close.

The "lens focus" dim (siblings to 0.35 opacity + `scale(0.96)`) is one `:has()` selector keyed off the same `.is-cross-hovered` class, matching the `:has()`-based mask-drop technique `ProjectReel`'s own hover-pop already established rather than a second mechanism. `prefers-reduced-motion` gets an explicit `transition: none` override (this file's established pattern for every other hover-triggered transition), even though a discrete hover response isn't really the ambient/auto-playing motion the guideline targets — the brief asked for it explicitly, so it's there regardless of that distinction. Verified live: font-weight and dim state both apply correctly on real mouse hover, real keyboard Tab-focus, and hovering the matching reel tile from the previous round's feature; siblings return to full opacity on mouse-leave; zero console errors.

**Visit/Year, round 3: the tags themselves always show now.** Follows a request that named five specific fake client names for the placeholder slots ("Estrela Studio," "Yucca Packaging," "Zulik," "PayJustNow," "Vineyard Hotel") — none real, none in this project anywhere, declined outright per PRODUCT.md's non-fabrication rule (see that round's own reply). This request is the legitimate part of it: the Visit/Year block previously disappeared *entirely* on a placeholder (gated on `project.url || project.year`, both unset), so it was never "already on each placeholder" the way it is on KrachtigFit. Fixed by separating the tag (always renders) from the value (still fully honest): a real `url`/`year` still shows the real link/value exactly as before; an absent one now shows a real, muted, non-link placeholder value instead — "Coming soon" for Visit (matching the exact voice `case-study-body`'s own fallback already uses — "Case study in progress — check back soon" — not a new tone invented for this), "—" for Year (the standard "no value yet" convention, not a guessed date). New `.case-study-infobar-value-muted` reuses `--color-ui-muted`, the same secondary/inactive color this system already uses for labels and flags, so a placeholder's Visit/Year reads as visibly "not real yet" at a glance, not as a dimmer version of a real value. Verified live on both a real project and a placeholder: KrachtigFit still gets its real clickable link + "2026"; every placeholder shows the same two tags, un-clickable, muted, zero console errors.

**Logo and wordmark link home.** Direct request, addressed to "the logo and the top-left brand name" as if they sit together — they don't: the logo was moved into the right-hand nav cluster (with About/Contact) by an earlier direct request this same session, and only the wordmark is actually top-left. "Preserve the exact layout coordinates" was explicit in the same request, so that actual layout was kept rather than moved back to match the request's own inaccurate premise — confirmed via real bounding-box measurement (wordmark at the established left inset, logo still in its right-hand cluster spot before About), not assumed from reading the JSX. `.wordmark` swapped from a plain `<div>` to `next-view-transitions`' `Link` with the identical className — a flex item's own outer display value doesn't affect a flex parent's layout, div and `<a>` get blockified the same way, so nothing shifted; needed one real addition, `text-decoration: none`, since a bare anchor defaults to underlined and the div never was. The logo got its own new `.chrome-logo-link` wrapper around the existing masked `<span>` — the span dropped `role="img"`/its own `aria-label` (now `aria-hidden="true"`) since the *Link* carries the one real accessible name for this control now; a nested img role would have double-announced "Voltair Studio" on top of the link's own name.

No custom `role`/`tabIndex`/`onKeyDown` anywhere, despite the brief asking for "keydown listeners for Enter/Space" — a real `<a href>` is natively focusable and already activates on Enter for free; Space intentionally still scrolls the page instead of navigating, which is correct native link behavior, not a gap. Synthesizing a Space-to-navigate handler would fight what a link is actually supposed to do on Space and surprise anyone using a screen reader or keyboard, exactly the "AI slop" the brief's own banned-list is really about even where its own literal ask pointed the other way. No new `viewTransitionName` tagging either — the wordmark/logo render identically on every page already (this isn't two different elements that should visually become each other, unlike the reel-tile-to-case-study-title morph), so the plain default transition `ViewTransitions` already gives every Link click is the correct, complete behavior here, not a gap to fill. Verified live: real navigation via click and via keyboard Enter from both `/about` and a case-study page back to `/`, clicking while already on `/` is a harmless no-op, rest-state color confirmed unchanged (a screenshot taken right after a `.click()` initially looked like a color regression — it was just the hover state the click itself left active, confirmed by re-checking with the pointer moved away first rather than assuming), zero console errors.

**"Boot Sequence, Persistence & Easter Eggs" — a real bug caught by not trusting a green build.** Three parts.

*Boot sequence*, aimed at `TerminalPane.tsx` (the layout wrapper — the real target was `TerminalInput.tsx`, which actually owns the log this plays into). Four short flavor lines print once on the very first visit ever (`voltair_studio deploy console` / `skybox engine ... ready` / `terminal shell ... ready` / `type 'help' to begin`) — real facts about what's genuinely already true by the time this runs (the skybox really has finished loading, LoadingScreen already gated on that; the terminal really is interactive), not invented subsystem names like the brief's own example text ("MOUNTING CHRONO-COSMOS," "STABILIZING GYROS"). This is *not* a second loading screen — `LoadingScreen.tsx` still owns the one real asset-gate; this just plays into the same LOG_LIMIT-capped log every command already uses, never blocks real typing, and is skipped outright under reduced motion.

Building it surfaced a real bug, caught by re-checking the actual dev server rather than trusting a clean `npm run build`: the first version checked *and set* a localStorage "seen" flag inside the same mount effect that scheduled the sequence. React 18 Strict Mode runs a mount effect, its cleanup, then the same effect again on every real mount (dev only, to catch exactly this class of bug) — the first invocation set the flag and scheduled four timeouts, its cleanup cleared every one of them before any could fire, and the second invocation then saw the flag already set and skipped entirely. Net effect: the sequence silently never played, on dev, while `npm run build`/`next build` stayed completely clean (Strict Mode doesn't double-invoke in production, confirmed directly against a real `next build && next start` — it played correctly there even with the buggy version). Fixed by deciding "should this play" once, during render, via the same lazy-init-ref guard `mountTimeRef` already uses a few lines above — both Strict Mode invocations agree on that decision, so the one that survives still gets to finish. Worth being honest about: this would have been easy to wave off as "well, the real build is fine" — an effect that only behaves correctly by accident of which environment happens to run it once isn't something to leave as a known dev-mode quirk.

*Persistence*. `--audio` was already real (see the earlier "Ultimate Console Polish" entry) — confirmed still working, not rebuilt. `skyboxStyle` needed a real design decision, not just a mechanical add: the active skybox already had a deliberate, from-scratch feature (a chrono-aware default matching the visitor's real local time of day, extensively documented earlier in this file) — blindly persisting "whatever's active" would have meant a first-time chrono pick got treated identically to a real manual choice, permanently freezing every later visit on whatever hour someone happened to first load the site. Fixed by persisting only from `next()` itself (the one function both the switcher and the terminal's `skybox` command already funnel through) and only reading the stored value back if one actually exists — a visitor who's never touched the control still gets the real time-of-day default every time, exactly as before; one who has gets their own choice remembered. The CRT scanline toggle didn't exist at all before this round (it shipped as an always-on, non-toggleable baseline commitment) — new `crt-context.tsx`, mirroring `audio-context.tsx`'s exact provider/hook shape, syncing its one boolean onto a real `document.body` dataset attribute the CSS reads (`body:not([data-crt="off"])::after`), plus a new `CrtToggle.tsx`/`--crt=on`/`--crt=off` terminal command pair, both matching the audio toggle's already-established pattern exactly rather than inventing a new shape for the same kind of control.

*Easter eggs*. `systeminfo` reports eight fields, all real: uptime is genuine time-since-mount (not the brief's own suggested "mock" number — real data was no more work than fake data here, so real is what shipped), viewport/DPR/canvas resolution read straight off `window`/the actual skybox `<canvas>`, engine is a best-effort `navigator.userAgent` parse (Edge/Firefox checked before the generic Chrome match, since both Edge's and Chrome's own UA strings contain "Chrome/" — a naive single regex would misidentify Edge), and audio/CRT/skybox all read the same live state their own toggles do. Rendered in its own fully-sized block, not routed through the regular log's LOG_LIMIT=2 cap — a real 8-row table needs more room than that cap was deliberately set to avoid a scrolling-log problem, but since every row here is a known, fixed count (never open-ended), it doesn't need that cap's own scroll-avoidance trick either, just a bigger fixed box. `theme <name>` swaps `--color-amber-bright`/`-dim` (plus the three hardcoded-rgba tint/underline tokens that can't reference a CSS variable inside their own alpha slot) via a `data-palette` attribute — framed and built as a terminal-only "secret command" easter egg, not a new user-facing StatusBar control, matching the brief's own "no bloated configuration panels" line. Green is a real classic-CRT phosphor green, kept same-hue-just-darker for its own dim variant (genuine monochrome-phosphor character, unlike persimmon's own unrelated slate-grey dim); amber reuses this project's own actual original pre-persimmon accent values from its very first design plan, not invented for this feature. Verified live: a manual skybox pick survives a reload instead of resetting to chrono; CRT toggling actually adds/removes the real background-image and the off state persists; `systeminfo`'s fields are genuinely live (uptime ticks, engine correctly reads as e.g. "Chrome 151"); `theme green`/`amber`/`persimmon` visibly recolor the entire site from one command and revert to the exact original color on `persimmon`, surviving a reload in between; zero console errors and zero overflow at the 320px floor with audio, a non-default theme, and systeminfo all active at once.

**Terminal prompt discoverability, closing the loop.** Direct follow-up: removing the `$` prefix and then the cursor glyph (both earlier direct requests) left the terminal line with genuinely zero visible affordance at rest — nothing signaled it was a real text box until a visitor happened to click it. Fixed with the studio's own logo mark, not new terminal-prompt symbols — the exact masked-recolor technique `.chrome-logo` already uses, same real `--color-amber-bright` token (so it follows a `theme` change too, verified live), sized off `--fs-position` rather than an `em` value since the logo and the mirrored text are siblings, not nested, so `em` there would've inherited `.terminal-prompt`'s own font-size instead of `.terminal-prompt-text`'s. `flex-shrink: 0` keeps it from ever getting squeezed as typed text grows. Verified at desktop, mobile, and the 320px floor: the mark is present and correctly sized at every width, the terminal is still fully functional (typing/Enter/commands all unaffected), zero overflow, zero console errors.

**Five more named themes — one real bug caught mid-implementation, one caught by a wider test after.** Direct request with five specific named palettes and their own hex values, framed as full background+text swaps ("white paper text on... royal blue", "Commodore navy", etc.), not just accent — reused the CSS-variable mechanism `theme green`/`theme amber` already established, deliberately **not** the background half: `--color-pane-bg` turned out to be load-bearing for `--text-halo`/`--text-halo-small` (the per-glyph legibility ring on every piece of text on the page), partly hardcoded `rgba()` rather than fully `var()`-driven — safely re-theming that across 5 new colors without risking real text legibility against the sky (which never changes, only the accent does) is separate work, not something to fold in silently. One of the five names, `ambergris` (#FFB300), was byte-identical to the already-shipped `amber` — registered as a real, typeable alias rather than a duplicate CSS block for the same color; `phosphor-decay`'s #33FF66 is close in spirit to the existing `green` but a genuinely different, more specific real reference (the DEC VT100's own phosphor), so it shipped as its own theme rather than being merged away.

Caught two real bugs, both by testing rather than trusting a clean build:

1. **While rewriting the `amber` block's comment, its actual color declarations got deleted** — the CSS custom properties never made it into the edited version, only the prose around them did. `theme amber`/`theme ambergris` silently fell back to persimmon with zero errors anywhere. Found immediately by the same live verification pass every round already runs (computed-style checks against real hex values, not just "did the command run without throwing") — not left for later.
2. **The bigger one: a saved theme only ever restored on the homepage.** The restore-on-mount effect lived inside `TerminalInput.tsx`, which is homepage-only content — a fresh load of `/about` or a case-study page never ran that effect at all, so it silently showed persimmon regardless of what was actually saved, even though localStorage genuinely held the right value. The first verification pass this round only reloaded the homepage and missed it entirely; caught by a second, wider pass that actually navigated to other pages after setting a theme — exactly the kind of check worth adding *because* the first one passed clean, not skipped because it did. Fixed by pulling the palette list, validation, and apply/persist logic out into a new `app/data/palette.ts` (shared source of truth, same reasoning as `projects.ts`) and adding `PaletteRestorer.tsx` — a small, contextless component whose only job is running that restore effect, rendered once at the root layout so it runs on every page, not just the one that happens to host the terminal. Verified both a hard reload of a non-homepage route and a real client-side `<Link>` navigation to one, both now carry the theme correctly; re-ran the full original theme-by-theme suite afterward to confirm the refactor changed nothing about how `theme <name>` itself behaves.

**A real fold-out theme picker on the terminal logo — and a real click-blocking bug caught along the way.** Direct request: hover the logo next to the terminal input, get a visual list of themes instead of needing to know/type `theme <name>`. This directly supersedes an earlier round's own reasoning ("no bloated configuration panels" — kept `theme` terminal-only, no visible switcher) — a direct ask for a visible one overrides that, it isn't a contradiction to build now. The logo (previously a plain decorative `<span>`) became a real `<button aria-haspopup>`; a `.palette-picker` wrapper wraps it plus a `.palette-picker-menu`, revealed via `:hover`/`:focus-within` — pure CSS, no new JS open/close state, the exact same reveal technique `.mockup-carousel-arrow`/`-dots` already established elsewhere in this codebase, chosen again rather than inventing a second one. The menu's own seven real theme names (every `PALETTES` entry except `ambergris`, which shares its exact color with `amber` and would just look like a bug shown twice) each get a small color swatch — genuinely duplicates the real hex values already in `globals.css`'s `:root[data-palette]` blocks rather than trying to read a CSS custom property back into JS or loosening the established `:root`-only selector scheme for one decorative nicety; kept in `app/data/palette.ts` (`PALETTE_PREVIEW_COLORS`) as the one place that duplication lives, not re-declared per component.

Caught two real bugs before calling this done, neither from just reading the code back:

1. **The button was completely unreachable by mouse.** `.terminal-prompt-input` is `position: absolute; inset: 0` over the *entire* prompt row — a real, generous click target for the terminal itself, built deliberately several rounds ago. With no `z-index` of its own, it painted over the new logo button sitting visually inside that same row (later in DOM order wins when neither side sets a z-index), silently swallowing every hover/click meant for the picker. Playwright's own hover attempt didn't just fail quietly — it named the exact blocking element ("input intercepts pointer events"), confirming the mechanism rather than leaving it as a guess. Fixed with `z-index: 1` on `.palette-picker`, re-verified the element actually under the cursor at the logo's real coordinates afterward rather than trusting the fix from the diff alone.
2. **The menu didn't close after picking a theme.** A clicked `<button>` keeps browser focus by default, and since `:focus-within` is one of the two things that reveals this menu, the just-picked item staying focused kept it open even once the mouse moved away — correct per the code, not the feel a normal dropdown has. Fixed by blurring the clicked item right after applying the theme, so selection now closes the menu the same way moving the mouse away already did, instead of only closing on a later, unrelated focus change.

Verified live end to end: the right element is actually hit-tested at the button's coordinates, hovering reveals the menu and moving away closes it, clicking a swatch applies and persists the real theme (color checked directly, not just "did the click land"), Tab onto the trigger reveals the menu with the very next Tab already landing inside it in visual order, Escape closes it, the terminal itself still works afterward, and zero overflow with the menu open at the 320px floor.

**`green` and `phosphor-decay`, actually made distinct.** Direct feedback: disliked both, and correctly — they sat at roughly 130-140° hue in HSL, close enough to read as the same green at a glance despite different hex values on paper. Fixed by actually separating them in hue space rather than nudging the numbers: `green` moved to a vivid chartreuse/lime (~100°, `#7cff3d`) for a punchier, more modern "hacker terminal" character; `phosphor-decay` moved cooler, to a mint/spring-green (~155°, `#2ee6a0`) — leaned into its own name doing the differentiating work too: a worn CRT phosphor's real afterglow reads as a softer, cooler decay next to the other theme's now-warmer, more vivid lime, not a second neon competing with it. Verified the actual hue separation directly (56°) rather than trusting that two different-looking hex strings are different-looking colors — the same mistake that shipped the original pair. Both swatch preview colors in the fold-out picker updated to match (`app/data/palette.ts`), so the picker's own preview never drifts from what clicking it actually applies.

**"green" recolored to blue — renamed, not just recolored, and reusing a real piece of this project's own history.** Direct request. A command literally called `theme green` making everything blue would be its own small trap, so this was a rename: `cobalt`. "Electric Cobalt" was one of this project's own real candidate accents weighed against persimmon before persimmon won (see palette v3, earlier in this file) — reused here rather than invented fresh, the same instinct that already gave `amber` its real pre-persimmon values. That history also flagged *why* cobalt lost the main-accent vote: it sits close in hue to the skybox's own blue tones, and an accent that near-matches its background loses contrast. Didn't reuse that exact muted historical value here for that reason — brightened into a more vivid, saturated electric blue (`#3d8bff`) instead, matching how every other bright accent in this system is highly saturated, so the same contrast concern that ruled cobalt out as the permanent brand color doesn't quietly reapply to it as an optional easter-egg theme. Every reference to the old name — `PALETTES`, the CSS override selector, `COMMANDS`, the swatch preview map, and the surrounding prose comments in three files — updated together rather than leaving the rename half-done in some of them; `phosphor-decay`'s own comment (previously explaining its hue relative to "green") rewritten too, since that comparison is moot now that the other theme isn't green at all. Verified live: the old `theme green` command is now correctly rejected (with `cobalt` listed among the valid options), `theme cobalt` and the fold-out picker's own `cobalt` entry both apply and persist the real blue, and tab-completion resolves the new name correctly.

**Palette picker menu, made actually glassy.** Direct follow-up, with a screenshot: the fold-out menu shipped using the diagonal repeating-gradient "Terminal Glass" recipe (the reel tiles/mockup card's own pattern), which read as an opaque textured box, not transparent. That was the wrong precedent to copy — the diagonal stripe pattern's real job elsewhere is signaling "placeholder, content goes here" on a large decorative surface; a small menu of live, real options was never that. `.mockup-carousel-arrow` (also a small real control, not a placeholder) already had the right recipe in this same file — a flat `background: var(--color-glass-fill)`, no gradient — swapped to that instead. Same blur, same border, same everything else; only the fill pattern changed. Verified the computed background is genuinely flat now (`background-image: none`) with real backdrop blur still active, and that the menu is still fully functional afterward (hover, click, theme applies).

**Palette picker menu, round two: still read as grey, not glassy.** Direct follow-up — the flat `--color-glass-fill` fix (previous entry) fixed the texture problem but not the actual complaint: at that token's own 0.35 alpha, the fill still read as a visible grey panel rather than something genuinely see-through to the skybox. Dropped to a scoped 0.12 alpha (a spelled-out `rgba(34, 40, 49, 0.12)`, not the shared token — `--color-glass-fill` is reused by several other real surfaces, the reel tiles and the carousel arrows/dots among them, so turning it down here would have quietly turned all of them down too) with the blur nudged up slightly (8px → 10px) to keep a real frosted definition at that much lower tint. Legibility wasn't a blind guess at this opacity: every label already carries `--text-halo-small`, the same per-glyph legibility ring every other piece of text sitting directly on the open sky across this whole site already relies on, so there was real, already-proven room to make the panel this much more transparent without the theme names becoming hard to read — confirmed by actually looking at a screenshot after, not just trusting the token math.

**`--audio`/`--crt` flag buttons had a visible box — a missing UA-reset, not a design choice.** Direct report, with a screenshot: the two toggle buttons showed a rectangular box the other `.flag`-styled controls didn't. Root cause: `.flag` is shared by real `<a>` tags (`--github`/`--x`/`--linkedin` — no native chrome to begin with) and real `<button>` elements (`AudioToggle`, `CrtToggle`), but `.flag` itself never reset the browser's default button chrome (background/border/padding/appearance) the way `.skybox-switcher` already does on its own separate class — so the anchors read as plain text while the buttons kept the OS's native grey bordered box. Fixed once, in the shared `.flag` rule (`background: none; border: none; padding: 0; font: inherit; cursor: pointer;`) rather than duplicating the reset onto `.audio-toggle`/`.crt-toggle` individually, so any future `.flag` button gets it automatically. The hover/focus/active tint washes (`--color-amber-tint-08`/`-14`) are untouched — those are the same intentional hover affordance every other `.flag`/`.chrome-nav-link`/`.cta` control in this system already uses, not the bug. Verified via computed style (`background-color: rgba(0,0,0,0)`, `border: none`, `padding: 0px` at rest on both) and a screenshot showing all six status-bar flags now render identically at rest, with the hover wash still intact.

**EN/NL language toggle.** Direct request, placed between the logo and About per the ask, planned before building (scope confirmed: translate everything, including case-study chrome, not just the top-level nav). Architecture matches the codebase's own established shape rather than reaching for an i18n library: `data/i18n.ts` is a flat `{ en, nl }` dictionary + `t(lang, key)` lookup (same "one shared source of truth" instinct as `palette.ts`/`projects.ts`), `lang-context.tsx` is a Context provider with the identical SSR-safe-default-then-localStorage-correction shape `crt-context.tsx`/`audio-context.tsx` already use (`lang="en"` renders first, corrects to a saved choice after mount — same one-tick gap those toggles already accept), and `T.tsx` is a tiny Client Component wrapper (`<T k="..."/>`) for the two Server Component pages (`about/page.tsx`, `work/[slug]/page.tsx`) that keep `generateMetadata`/`generateStaticParams` and so can't call `useLang()` directly — same "extract just the client-needing part" shape `EstrelaCardViewer` already uses on the same case-study page. `about/page.tsx`'s translatable body was pulled into a new `AboutContent.tsx` client component instead, since it's a whole paragraph plus several labels rather than a few standalone words.

Scope boundary decided once, not per-string: real terminal command tokens (`help`, `about`, `theme cobalt`, `--crt=on`, ...) and `systeminfo`'s technical field names (`uptime`, `viewport`, `dpr`, ...) are never translated — a real shell's own commands don't change with the OS's display language, and neither do a diagnostic tool's field names. Only the prose around them (boot lines, log framing like "command not found" / "→ sound:", help/error text) and real page copy (headline, tagline, About's paragraph, case-study labels and placeholder text) are bilingual. `about --voltair_studio`'s `exit` link is the same kind of command token and stays untranslated for the same reason. KrachtigFit's real mockup captions (`Over mij`, `Klanten resultaten`, ...) are already Dutch — they're honest labels of what that live site's own screenshots show, not translatable UI copy, and were left alone.

The Dutch strings are a real first-pass translation, not a stub — but professional brand copy in a second language is worth a native speaker's read before this ships live, the same caution PRODUCT.md's own "[Assumption, flagged for confirmation]" tag already models for unverified claims elsewhere in this project. Verified live: the toggle switches `document.documentElement.lang` and every wired string immediately, the choice survives a hard reload and persists across `/`, `/about`, and `/work/[slug]` navigations (same cross-page restore fix `PaletteRestorer` needed), and `ScrambleText`'s decode animation correctly replays on the new text when the headline's words change language (its effect already depended on its `text` prop) rather than leaving stale English glyphs on screen.

**Tagline NL fix + terminal prompt border, both direct follow-ups.** The Dutch tagline shipped as "De architectuur achter de esthetiek — geëngineerd." — reported back as an em dash awkwardly splitting the sentence, not idiomatic. Rewritten as one continuous clause, "De geëngineerde architectuur achter de esthetiek.", same meaning as the EN gerund-led original with no punctuation break; the EN copy's own em dashes elsewhere (`about.body`, `work.inProgress`, `terminal.contactCopied`) were left alone since those genuinely introduce a second clause in both languages (a normal appositive, not two words with a dash wedged between them) — checked each one individually rather than stripping every em dash in the dictionary on one report.

Separately: the terminal input row (`.terminal-prompt`) had no border at rest, only `:focus-within`'s outline — direct request to make it "always visible," not just on focus. First pass reused `--color-glass-border` (the neutral token every other bordered surface in this system already shares) plus `padding-inline` for real breathing room now that a border sits at the edge. Checked live against the actual sky before calling it done, per this project's own standing discipline of confirming visual changes by screenshot rather than trusting the token on paper — and at rest it read as basically invisible: that token's 10%-alpha neutral white sits too close to the sky's own light color to register as a highlight. Swapped to `--color-underline-rest` instead — the accent hue at 0.35 alpha, the same "visible but not shouting" rest-state token `.chrome-nav-link`'s own underline already uses — which reads as a real highlight at rest and re-themes automatically with `theme <name>` since it's already a per-palette custom property. Verified the fold-out picker menu (anchored to a child of this row, not the row's own edge) still opens in the right place with the added padding.

**About page's floating 3D logo.** Direct request, with a real asset supplied (`VoltairLogo3D.glb`, moved into `public/models/voltair-logo.glb` — same convention as `public/skyboxes`/`public/mockups`/`public/photos`). Lives in `SkyboxCanvas.tsx`'s existing scene/renderer rather than a second canvas: `.about-page-content` already centers its text directly over the open sky with no panel behind it, and `.skybox-canvas` already sits at `z-index: 0` under everything, so a world-space object in that same scene shows through exactly the same way with zero new stacking or layout work — the "floating behind the text" part of the request came for free from decisions already on record, not new plumbing.

Loaded on demand via `GLTFLoader`, not eagerly at mount: this canvas is shared by every route (homepage included, the highest-traffic page), and the file is a real ~6MB — a `[pathname]`-keyed effect only fires the fetch the first time a visitor actually reaches `/about`, gated by a ref reset at scene-creation time (not in cleanup) so a Strict Mode dev double-invoke — which rebuilds the whole scene — still gets its own fresh load instead of skipping it because the previous, now-discarded scene's request already flipped a shared flag; the async `.load()` callback checks the *current* `sceneRef` by identity before touching anything, so a load that resolves after the scene it was for has already been replaced or torn down disposes what it decoded instead of adding it to a scene nothing will ever render again. The model's own export scale/pivot was never trusted (Blender units and pivot points are arbitrary per file) — measured via `Box3.setFromObject` and normalized to a fixed target size instead of assuming either. Two lights (ambient + one directional) were added to the scene unconditionally: the sky sphere's `MeshBasicMaterial` is unlit by design and completely unaffected by them, but the glTF's own PBR materials render solid black with no light in the scene at all — confirmed live, not assumed, since a black silhouette and a missing model look identical in a screenshot.

First pass sized the logo to be a real presence (radius 4.5 at distance 24) — checked live and it read as a second foreground object competing with the paragraph, not something floating behind it: `.about-float`'s text is left-aligned, but its *panel* is screen-centered same as the logo, so the two shapes overlapped across the whole reading column. Sized down and pushed back (radius 2.6 at distance 30) plus slightly dimmed lighting reads as background presence instead — `--text-halo` was already keeping the text legible either way, but the ask was "behind," not just "still readable." Gave it its own idle motion (a slow self-spin on a distinct period from the sky's own 150s rotation, plus a gentle vertical bob) so it reads as "floating," matching the word the request used, not a static prop. Verified live: the model fetches exactly once on first reaching `/about` (confirmed via network listener), does not fetch at all from the homepage, and does not re-fetch on leaving and returning to `/about` via real `<Link>` clicks (client-side navigation — confirmed separately from a `page.goto()` hard reload, which understandably does re-fetch everything since it tears down the whole app); no console errors on load, and a screenshot confirmed real shaded geometry (not a black silhouette, not a missing-texture placeholder).

**Logo sized up again, direct follow-up.** "Increase its size a lot more" — radius 2.6 → 9 (distance unchanged at 30), no other change; the previous round's dial-down was itself a direct decision to make, not a hard ceiling, and this simply overrides it with a further direct instruction. Checked live at both a 1440px desktop width and a 390px mobile viewport for clipping/overflow before calling it done, since a change this size-sensitive is exactly the kind that can silently blow past a frame at one viewport while looking fine at another — held at both.

**Logo sized up a third time, direct follow-up.** "Make it bigger" — 9 → 13. At LOGO_DISTANCE 30 and the camera's 75° vertical fov, the visible half-height there works out to ~23 units, so 13 was deliberately picked close to that ceiling rather than another arbitrary jump — checked live afterward at the same two viewports as the previous round to confirm the bounding sphere still clears the frame top-to-bottom rather than trusting the math alone (portrait width doesn't add headroom; fov is vertical). Still real room before it would actually clip if a future round asks for bigger still.

**Logo surface smoothed, direct follow-up.** "Smoothen the glb ... so it looks smoother" — checked live with a close-up screenshot first rather than guessing what "smoother" meant, and it showed real polygon faceting: flat patches visibly breaking up what should be a continuous curved highlight along the flame's curl, not a stylistic choice worth defending. Root cause lived in the export, not the renderer: a flat-shaded GLB duplicates each triangle's vertices so every face can carry its own single normal — which is exactly what defeats a plain `geometry.computeVertexNormals()` call taken alone, since there's nothing shared to average across yet. `smoothLogoSurfaces()` strips the existing normal attribute first (so `mergeVertices()`, from `three/examples/jsm/utils/BufferGeometryUtils.js`, welds purely on position instead of also requiring the old, deliberately-mismatched per-face normals to already agree), then computes fresh smooth normals from the newly-welded topology; also clears `flatShading` on any `MeshStandardMaterial` found, the same defect from the other direction some exporters set directly. Runs once per mesh right after load, before `normalizeLogo`'s own centering/scaling pass — a one-time cost on an already-lazy-loaded asset, not a per-frame one. Verified with a second close-up screenshot at the same crop: the highlight bands now read as one continuous curved reflection instead of segmented flat facets, no console errors from the geometry rewrite.

**Logo material/lighting polish.** Follow-up to the "make it smoother" round, offered as a next step and greenlit ("Step 4 you can do sure"). Inspected the loaded material live before touching anything (via a temporary `window` handle, removed once the read was taken) rather than guessing at a fix: `metalness: 1`, `roughness: 1` (a `roughnessMap` supplies the real per-pixel variation), `hasEnvMap: false`. A fully metallic material has almost no diffuse response — its entire look comes from what it reflects — so with no environment at all, two direct lights could only ever produce a single specular highlight sweeping across the surface, exactly the flat result already on screen. Real fix, not a hack: `RoomEnvironment` (`three/examples/jsm/environments/RoomEnvironment.js`) is three.js's own standard small procedural studio backdrop for lighting PBR previews, prefiltered through `PMREMGenerator` for correct per-roughness blur — reused as-is rather than sourcing a real HDRI or hand-rolling a fake environment. Generated once at scene setup, not tied to the active skybox theme: a generic studio reflection reads fine under any sky, and regenerating it (a real render pass) on every skybox switch would have been unjustified extra cost and complexity for no visible gain. Paired with `ACESFilmicToneMapping` on the renderer so the material's now-real reflections roll off instead of blowing out to flat white — a renderer-wide setting by necessity, but the sky sphere's own `MeshBasicMaterial` is explicitly exempted (`toneMapped = false`) so the actual skybox artwork keeps rendering exactly as before. Direct light intensities were also trimmed down (they were tuned for a no-environment scene; left unchanged, the added environment would have doubled up on top of them). Verified live: a before/after close-up shows real dimensional metal — soft gradient falloff and rolled-off highlights instead of one flat sweep — and a homepage screenshot (a route the logo never renders on, but the tone-mapping change is renderer-wide) confirms the sky itself is pixel-for-pixel unaffected.

**Logo sized up a fourth time, direct follow-up.** "Make it just bigger" — 13 → 18, LOGO_DISTANCE unchanged at 30. The previous round's own comment had called 13 "close to the ceiling" (the ~23-unit visible half-height at that distance), but that was framed from the bounding sphere's radius, not the flame shape's own actual silhouette — which isn't a tight fit inside that sphere, so there was real headroom left. Checked live at both the desktop and mobile viewports again rather than trust that margin on paper; both still clear the frame with no clipping.

**Second real case study: SOL_DNB // terminal club.** A Studio-panel prompt asked to "replace the second project placeholder (Yucca Packaging)" with this — no such placeholder has ever existed in this project (both name and premise match the exact shape of an earlier prompt this session declined, which invented 5 fake client names for these same slots), so the request was checked, not built on faith. `soldnb.com` turned out to be a real, live site whose own footer reads "designed by VOLTAIR_STUDIO" — a genuinely different situation from the declined one, confirmed before writing anything, not assumed either way. Every feature the request described (Kick/Twitch stream monitoring, a session-gated track request queue, a `playtool_synth_v2` waveform module) was independently verified present on the real page rather than taken from the request's own copy.

Built the same way KrachtigFit was: `PROJECTS[1]` now holds slug `soldnb`, the real `https://soldnb.com` URL, a `year` of 2026 confirmed independently from the site's own footer copyright line (not just the request's claim), and real full-viewport screenshots — one per real section, captured live and named with that section's own real on-page heading, matching krachtig-fit.png's own capture technique rather than a tighter/different crop. (The reel/index tile and the exact mockup set both changed in direct follow-ups below.)

`Project.description` is now `{ en, nl }` instead of a plain string — the first real per-project description written since the EN/NL toggle shipped (KrachtigFit's was deliberately left unset before any translation system existed), so nothing had exercised this path yet. Rather than grow `data/i18n.ts`'s shared UI-string dictionary with one-off project content, added a small `<Localized en nl>` component (`Localized.tsx`) that does the same "pick the current language" job `<T>` does, just for inline text instead of a fixed dictionary key — keeps case-study content and site chrome in separate places. `role` stays a single untranslated string on purpose, matching how project names themselves also aren't translated.

Extended the terminal's `work` command to take an optional slug (`work soldnb` → `router.push("/work/soldnb")`, the same real-navigation pattern `about` already uses) — this genuinely didn't exist before; bare `work` only ever listed projects and focused the reel. Didn't add per-slug entries to the static `COMMANDS` array (used for `help`'s listing and tab-completion) — the ask was that typing the full command works, not that it tab-completes, and 8 more literal entries there would have cluttered `help`'s output for a generic capability the command's own error message and `work`'s no-arg listing already surface.

**SOL_DNB tile photo + mockup re-curation, two direct follow-ups.** First: a real photo of Sol was dropped into `Logo/` (the project's general asset drop-in folder) with a direct request to use it as the reel/index tile instead of the site-hero screenshot the tile shipped with — moved to `public/photos/soldnb-sol.jpeg` (not left duplicated in `Logo/`, same move-not-copy handling the earlier GLB drop-in got) and swapped into `Project.image`, the exact same "real photo of the person, not a screenshot" choice `krachtig-fit-sander.jpg` already made for its own tile.

Second: direct feedback that the original 5 mockups (all cropped mid-scroll off one long homepage, one of them straddling two sections) "just [weren't] great" — replaced with 4 clean, full-viewport captures of 4 actually distinct destinations named explicitly: the homepage hero, the two real sub-routes (`/about`, `/schedule`) the first capture pass never visited at all, and the socials panel. `/about` and `/schedule` turned out to carry real, substantial content of their own (a full biography with real gear specs, a real broadcast schedule) — read in full before capturing, not just screenshotted blind. The now-unused originals (the old hero/hero-adjacent shot and the 3 dropped panels) were deleted from `public/mockups/`, not left as orphaned assets alongside the new ones. Verified live: all 4 new images present in the carousel's DOM in the intended order and each returns 200; the tile's `<Image>` `src` resolves to the real photo through Next's own optimizer, not the old screenshot.

Verified live end to end: no "Yucca Packaging" anywhere in the rendered homepage; hovering "SOL_DNB" in the index triggers the kinetic expansion and highlights the matching reel tile; clicking it produces the real `next-view-transitions` morph to `/work/soldnb`; typing `work soldnb` in the terminal navigates there too; the case-study page shows the real description in both languages, the real Visit link, and the real Year; zero console errors.

**KrachtigFit's own description, direct follow-up.** Asked to match SOL_DNB's own description style once that one shipped. Re-checked the real live site before writing anything, same as SOL_DNB's own diligence — not worked from memory of the mockup carousel's section labels, which don't carry the actual program details. Landed on the three real, structural facts (the strength/nutrition/combined program tracks, weekly check-ins, the free "kennismaking" consultation as the entry point), in the same one-sentence em-dash-list shape SOL_DNB's description already uses. Deliberately left out the site's own "500+ clients coached" claim: that's a stat/proof point, exactly the category PRODUCT.md says not to assert without independent verification — a different kind of fact than "the service has three program tracks," which is just describing what's structurally on offer. Filled in the same `{ en, nl }` shape `Project.description` already uses. Verified live in both languages via the case-study page's own rendered text, not just read back from the source file.

**Console triage: a real bug fixed, a real dev-only limitation identified, two false leads ruled out.** User pasted raw browser console output and asked about it — triaged each line rather than reacting to the dump as a whole; several were unrelated browser-extension noise (`content.js`, "No Listener: tabs:outgoing.message.ready") that don't originate from this app at all.

Real, fixed: `ProjectReel.tsx`'s wheel handler used React's `onWheel` prop and called `e.preventDefault()` — modern React registers wheel listeners passively by default (matching the browser's own default, for scroll-performance reasons), so that call was silently a no-op, producing "Ignoring 'preventDefault()' call on event of type 'wheel' from a listener registered as 'passive'." `.page`'s own `overflow: hidden` happened to backstop any leaked scroll, so nothing was visibly broken, but it's a real warning with a known, standard fix: moved the handler to a native `addEventListener("wheel", ..., { passive: false })` in a `useEffect`, the same pattern this file already uses elsewhere for listeners needing non-default options. Verified live: warning gone, manual wheel-scroll on the reel still works, page still doesn't scroll.

Also real: a Next.js dev-mode warning flagged `krachtig-fit-sander.jpg` as the detected LCP image needing eager loading. `ProjectReel`'s marquee duplicates every project 6× for the infinite-loop illusion (see this file's own top comment) and mounts already scrolled to copy index 1 — confirmed empirically (not assumed) which exact DOM instance is actually visible at paint time, then marked only that one instance `priority`, not all 6 duplicates copies (which would trade one console warning for 6× the real, wasted eager fetches — a worse outcome than the warning itself). The fix is correctly applied and does help real production LCP timing for the tile that's actually on screen — confirmed via the raw SSR'd HTML — but Next's dev-only heuristic can't tell "a duplicate DOM copy used for a scroll illusion" from "real separate content," so it likely still fires in `next dev`'s console regardless of which specific copy is marked; confirmed this warning does not appear at all against a real `next build && next start` production server, so it never reaches an actual visitor's console.

Investigated, not chased further: a WebGL "drawElementsInstanced: Drawing to a destination rect smaller than the viewport rect" warning was pasted — that exact message format is Firefox's, not Chromium's (confirmed by trying to reproduce it in both engines via Playwright), so Chromium testing alone couldn't have caught it. Timing (fires once, only after the About page's logo scene is reached) points at `PMREMGenerator`'s own one-time internal cube-face render pass added in the material-polish round — a widely-documented, benign three.js/PMREMGenerator artifact from its internal viewport handling, not a sign of an actual rendering defect; found no visual symptom to chase, and three.js's own `PMREMGenerator` is designed to save/restore renderer state around it. Reported as a known, harmless one-time cosmetic quirk of a library utility rather than invented a fix for something not actually broken.

Ruled out: a "Security Error: Content at http://localhost:3000/work/soldnb may not load or link to file:///" line. Grepped this entire codebase for any `file://` reference — zero matches. Nothing in this app ever constructs or links to a local-file URL; every asset reference here is root-relative (`/mockups/...`, `/models/...`) or a real `https://` URL. Attributed to the same browser extension generating the noise immediately surrounding it in the pasted log, not this app.

**About page rewritten: the real "This Is Voltair" essay, and a zero-scroll-to-real-scroll conversion.** Direct request, with a real source file (`This Is Voltair/This Is Voltair by Bennie.txt`) — the user's own first-person writing about running Voltair Studios, not something to verify externally the way a client project's own claims get checked; light copy-editing only (a couple of typos, one added dash matching the surrounding sentences' own punctuation) preserves the actual content and voice untouched. Structured into `data/about-content.ts` (kicker, lead paragraphs, a Philosophy section with 3 subsections, a "What I've Been Building Lately" section linking the two real case studies by their real `/work/[slug]` routes instead of routing visitors off-site, an Outro section, a closing line) rather than one long string, so `AboutContent.tsx` can give "each head piece... the correct colour" per direct request: `.about-section-title` (Philosophy / Building / Outro) bigger and bolder than `.about-subsection-title` (the 3 philosophy subheads), both amber-bright but visually distinct tiers; project names styled as real underlined links, not static subheads, since they're the one heading-level element that's actually clickable. Translated to NL too, same first-pass status as every other Dutch string on this site right now.

The content was long enough (a real multi-section essay) that keeping `/about` on `.page`'s zero-scroll grid was no longer viable without either unreadably small type or clipped content — converted it to a real scrolling page, reusing the exact shell `/work/[slug]` already proved for its own long-form content rather than inventing a second pattern: renamed `.case-study-chrome`/`.case-study-footer` to `.scroll-page-chrome`/`.scroll-page-footer` (shared by both pages now, not case-study-specific despite the old name) and gave `/about` a fresh `.about-page` content shell modeled on `.case-study`'s own reasoning (`position: relative` to beat the fixed skybox canvas, padding reserving space for the now-fixed chrome/footer). Added a real top-of-page back link (`.about-back`, mirroring `.case-study-back`) alongside the existing bottom "exit" — a page this long benefits from a return path near the top too, not just after scrolling all the way down.

One consequence worth recording, not a bug: the About page's floating 3D logo (`SkyboxCanvas.tsx`) renders on the skybox's `position: fixed` canvas, which doesn't scroll with the page — so on this now-scrolling essay, the logo stays pinned at the same point in the viewport as the visitor scrolls, floating behind whichever section currently fills the screen rather than only the first viewport. Checked live rather than assumed, and it reads well (closer to a persistent watermark than a problem) — the code comment explaining this was updated to describe it accurately, since the old one specifically named the removed `.about-page-content` centering behavior as the reason it worked.

Verified live end to end: real scrolling confirmed on `/about`; every section/heading/project renders correctly in both languages; the SOL_DNB project link resolves to the real `/work/soldnb` case study; and — since the shared shell rename touches `/work/[slug]` too — confirmed that page's own chrome/footer and layout are still pixel-identical after the rename, and the homepage (untouched by any of this) still renders normally. No console errors.

**"What I've Been Building Lately" section removed, direct follow-up.** Dropped the whole section — heading, both project blurbs (SOL_DNB, KrachtigFit), and their real `/work/[slug]` links — from `data/about-content.ts` in both languages, and its now-dead JSX block from `AboutContent.tsx`. Also removed the CSS that only that section used (`.about-projects`/`.about-project`/`.about-project-header`/`.about-project-title`/`.about-project-status`) rather than leaving unreferenced rules behind. Verified live: the section and both project names are gone from the rendered page, the Philosophy section now runs straight into The Human Outro with no gap, and everything else on the page — the other headings, the essay's remaining structure — is untouched.

**Logo GLB replaced with a new export.** Direct request: a new file (`Logo/VoltairLogoDef.glb`) dropped in to replace the one at `public/models/voltair-logo.glb`. Same path, so `SkyboxCanvas.tsx`'s `LOGO_MODEL_URL` needed no change — old file removed, new one moved into place (not left duplicated in `Logo/`, same handling every other asset drop-in has gotten this project).

Flagged before swapping, not after: the new export is 70.8MB versus the old file's 6.1MB — inspected the GLB's own binary structure (Node reading its JSON + buffer-view chunk headers directly, not a guess) rather than just quoting the file size difference: ~53MB of that is raw, uncompressed geometry (no Draco compression, a real red flag for a hand-authored model — that's an enormous vertex count for what's still visually one flame/ribbon shape), the rest is 4 embedded JPEG textures. Even lazy-loaded (this only ever fetches once a visitor actually reaches `/about`, unchanged), a file this size means a real, long wait on a typical connection — asked directly whether to compress it first (Draco + texture resizing via `gltf-transform`) or ship as provided; direct answer was to ship it as-is, so no compression pass was done. This is a live, known trade-off, not an oversight — worth reconsidering later if load time on `/about` becomes a real complaint.

The existing pipeline needed zero code changes for the swap: `normalizeLogo`'s bounding-box measurement and `smoothLogoSurfaces`'s generic mesh/material traversal were already built to not assume anything about a specific model's scale or shape, and both handled the new geometry correctly on the first try. Verified live: the model still loads (single request, 200, no console errors) and renders as real shaded geometry — a visibly different, more refined shape than the old export, not a black silhouette or a missing-texture placeholder.

**Logo GLB actually compressed, direct follow-up.** Asked afterward whether compression would change visual quality; answered honestly (Draco geometry compression quantizes vertex precision, imperceptible at this model's on-screen size; texture resizing would only matter if the model were ever shown much larger) and given the go-ahead. Inspected the file with `gltf-transform inspect` first rather than guessing: ~1,004,553 vertices / ~1.94M triangles at full float32 precision (the actual driver of the 53MB geometry chunk), plus 2× 4096×4096 and 2× 2048×2048 embedded JPEGs (~15.4MB).

Two real findings, not silently glossed over:
- `gltf-transform optimize`'s own defaults include mesh **simplification** (real polygon reduction) alongside compression — this is exactly the kind of fidelity trade-off already flagged as something not to do without asking, so it was run with `--simplify false` explicitly rather than accepting the tool's default.
- Texture resizing/recompression (`resize`, `optimize`'s `textureCompress` step) failed outright on this machine — a real, reproducible `sharp`/libvips `VipsInterpretation` colourspace error, hit identically on every texture, on two independent attempts (the project's existing install, and a from-scratch isolated install with a version-matched `sharp`) — an environment-level native-binding issue, not a config mistake. Not chased further past two genuine attempts; textures shipped unresized.

Draco geometry compression alone (`gltf-transform draco`, no texture step) took the file from 70.83MB to 19.86MB — real, verified, not assumed. This is a hard functional requirement, not just a size win: three.js's `GLTFLoader` cannot decode Draco-compressed meshes without a `DRACOLoader` attached, so shipping the compressed file without wiring one up would have broken the model's load entirely, not just changed its size. Added `DRACOLoader` to `SkyboxCanvas.tsx` (decoder files copied from three's own bundled copy into `public/draco/`, served from this app's own origin rather than three's default of fetching them from a Google-hosted CDN at runtime — one less third-party dependency), created once per scene alongside the renderer and disposed in the same cleanup every other GPU resource in this file already gets. `public/draco/`'s vendored, generated Emscripten/WASM wrapper code isn't this app's source and was never meant to be linted — added to `eslint.config.mjs`'s `globalIgnores` rather than leaving it to trip the linter or hand-waving past real lint errors. Verified live: the WASM decoder actually fetches and runs (confirmed via network listener, not just "the model appeared"), zero console errors, and the rendered shape/shading is visually identical to the uncompressed version — confirming Draco's quantization really is imperceptible here, not just assumed to be.

**About page redesigned into an editorial 3-column spread, real zero-scroll again — a Studio-panel prompt this time mostly wrong about the codebase, but pointing at a real ask underneath.** The brief assumed Tailwind (`columns-1 md:columns-2`, `h-screen`, `max-w-prose`) — this project has never used it, confirmed against AGENTS.md and every file touched all session — and assumed the current page was "one long centered line" wasting lateral space, which didn't match what was actually shipped (left-aligned, already a real ~60ch column). Most importantly, it asked for the exact zero-scroll constraint that had been *deliberately removed* from this same page earlier this session, on purpose, because the real essay is ~3 screens of content at 1920×1080. Flagged all three mismatches directly rather than building on a wrong premise, and asked how to proceed given the real conflict — direct answer confirmed zero-scroll was in fact still wanted, so the actual problem was "reflow ~3 screens of real text into 1" without cutting any of it, not "delete the essay to fit an arbitrary rule."

Solved with a real editorial layout, not raw CSS multi-column text reflow (which would split paragraphs and headings unpredictably mid-thought) — a 3-column CSS Grid where each column holds one whole, coherent content block (lead+meta, Philosophy, Outro), the way a real magazine spread groups sections side by side rather than free-flowing character by character. Type sizing switched from `dvw`/`dvh`-only fluid clamps to `dvmin` (already this system's own established convention elsewhere, and for good reason — checked live and a `dvh`-only size badly misjudged tall-narrow tablet viewports, sizing text for the available height while ignorant of how little width was actually there to hold it, and overflowing as a result). Breakpoints: 3 columns above ~980px, 2 (lead merged into the philosophy column's slot) down to ~520px, single column below that.

Iterated against real measurements the whole way, not assumption: 8 representative viewports (1920×1080 down to 320×568), checked via actual `scrollHeight`/`clientHeight` comparisons, not eyeballing. Desktop through small-tablet (5 of 8 — roughly 700px width and up) now hit **exact** zero-scroll after several rounds of real tightening (line-height 1.6→1.3, tighter gaps throughout, smaller heading scale) — the last few tablet-range pixels closed by shrinking `.about-float-close`'s font size, which had been left at the old scrolling page's larger scale.

Two real bugs caught and fixed mid-round, not shipped:
1. `.about-back` was still `position: fixed` at an offset tuned for the old (larger) top padding; tightening that padding in this same round moved the masthead up without moving the back link in step, producing a real, visible text overlap at the top of the page (caught in an actual screenshot). Fixed by moving it inline into `.about-masthead`'s own flex row instead of keeping two independently-hardcoded positions in sync — it never needed to be fixed-position in the first place once the page stopped scrolling.
2. The phone-width single-column fallback (`.about-grid` switching to one stacked column below ~520px) showed what looked like overlapping text in a screenshot. Chased it properly rather than trusting either the screenshot or a first "looks fine" measurement: `getBoundingClientRect()` on each `.about-col` container disagreed with the actual position of its own last child — a real flexbox pitfall, not a screenshot artifact. Flex items shrink by default (`flex-shrink: 1`); a flex column shorter than its 3 children's combined natural height was compressing each `.about-col` below what its own text needed, and since the text itself can't shrink, one column's words visually spilled into the next column's territory. `flex-shrink: 0` on `.about-col` within that breakpoint fixed it — the resulting genuine overflow is exactly what `.about-grid`'s own `overflow-y: auto` was already there to handle honestly, and post-fix measurements showed the "overflow" number *increase* (the true, previously-hidden-by-compression height finally being reported correctly), which is the fix working, not a regression.

Below ~520px (phones), true multi-column reading doesn't work at all — even 2 columns is reading through a straw — so it stacks to one column with a real, contained `overflow-y: auto` fallback rather than either cutting content or shrinking type past this system's established readability floor. This is a disclosed, deliberate compromise for the narrowest real devices, not silent scope creep back into scrolling: `.about-page`'s own `overflow: hidden` keeps it scoped to just that one region, the rest of the page still doesn't move.

**About page fully rebuilt again, corner layout replacing the 3-column spread — a long round of direct follow-ups.** The 3-column essay above was itself later judged "too long and too difficult to space out properly"; the user shortened it himself to a 3-paragraph bio plus a short tools list, and the page was rebuilt from scratch around a reference screenshot (segerman.dev's own About page) instead: a big "ABOUT" headline (reusing `--fs-headline` itself, not a re-derived clamp, so it's genuinely the same scale as "Creative Designers," not an approximation — later given the same `ScrambleText` mount-decode + hover-rescramble and blinking cursor block the home page headline has, both direct follow-ups once the static match wasn't judged to be "the same" yet), a bio column and a Tools/status/contact meta cluster split left/right (swapped sides once after the user saw the first mirrored version live), and a smaller "About --Voltair_Studio" title + kicker byline reintroduced above the bio once the big headline took over the H1's old role. The redundant "Available October 2026" the shared `StatusBar` footer now duplicated was suppressed on this one route via an optional `hideAvailability` prop, not by forking the component. "Voltair_Studio" was later capitalized (V/S) site-wide — every rendered instance of the underscore handle, not just this page's own copy of it.

Real bugs caught and fixed along the way, each live-verified rather than assumed fixed: a subgrid row-alignment pass (later superseded by this rebuild) hit `mergeVertices()` throwing on an interleaved vertex buffer from a `gltf-transform` re-export, traced to a silent app-level error swallow rather than a loader bug; `.about-grid`'s own `overflow: hidden` was clipping the leading glyph's legibility halo at the column's flush-left edge (fixed with a paired negative-margin/padding trick that moves the clip boundary without costing layout space); and the stacked mobile breakpoint's two blocks summing height instead of taking whichever was taller landed content flush against the fixed status bar with zero margin, tightened rather than left to trust the reserved padding alone.

## Do's and Don'ts

### Do:
- **Do** keep all body/label text in the amber family (bright or dim) — never introduce a gray neutral for "de-emphasized" text.
- **Do** compute any future `ch`-based grid track in the same `font-size` context as the text it sizes for — a real, recurring bug class in this system (a service-manifest component that once lived here hit exactly this, before it was cut for density; the lesson outlives the component).
- **Do** hold interactive touch targets at a real 44px `min-height`, not just adequate padding, so it survives the smallest type-scale floor.
- **Do** theme browser-default surfaces (`::selection` is themed to the amber-tint/cursor-flash pairing) rather than leaving them at browser defaults — the cheapest signal a page was actually built for this world.

- **Do** re-check `.content`'s text against every skybox (visually, since it has no flat-color background to contrast-compute) whenever a new skybox is added or `--text-halo` changes.
- **Do** remember the edge-pixel contrast lesson if any element ever gets a background again: an 0.86-alpha scrim (an earlier version of this system, since retired) looked fine by eye and measured as low as 2.65:1 at a real edge — only pixel sampling caught it.

### Don't:
- **Don't** let the logo's green appear anywhere but the real logo image itself — it is a brand-asset color, not a site accent, and simulating it with a CSS color token (as the old "online" LED did) or using it a second place anywhere spends a budget this system only has once.
- **Don't** add a second type family for "contrast" or "hierarchy" — every hierarchy step in this system is weight and size within one monospace family.
- **Don't** add drop shadows, gradients, or rounded cards to extend this system — it is flat, hard-edged, and monochrome-phosphor by commitment, not by omission. One scoped exception exists by direct request: the white→slate gradient text fill on "Creative" (see Typography → Headline entrance). Don't generalize that exception to other elements without the same kind of explicit direction it came with.
- **Don't** let any new element grow the page past `100dvh`/`100dvw` — the fluid `dvmin`-based scale exists specifically so nothing needs a scrollbar, ever.
- **Don't** let the skybox touch the terminal's own material language — it stays a backdrop the console is a viewport onto, never a texture, gradient, or lighting cue applied to the terminal's own components.
